destroy_the_corrupted_sunwell = {
	picture = { reference = "gfx/interface/illustrations/decisions/sunwell_active.dds" }
	decision_group_type = major

	ai_check_interval = 0

	desc = destroy_the_corrupted_sunwell_desc

	is_shown = {
		is_independent_ruler = yes
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:destroy_the_corrupted_sunwell
			}
		}
	}

	is_valid = {
		has_faith = faith:cult_of_sunwell
		is_independent_ruler = yes
		completely_controls = title:c_sunwell_plateau

		title:b_sunwell_plateau.title_province = { # Is there even a corrupted Sunwell to be destroyed?
			has_building = sunwell_corrupted_death
		}
	}

	cost = {}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_at_war = no
		is_incapable_trigger = no
	}

	effect = {
		save_scope_as = destroyer
		if = {
			limit = { title:b_sunwell_plateau.title_province = { has_building = sunwell_corrupted_death } }
			custom_tooltip = {
				text = corrupted_sunwell_being_destroyed
				trigger_event = wc_quelthalas_events.100
			}
		}
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:destroy_the_corrupted_sunwell
			}
		}
	}

	ai_potential = {
		is_imprisoned = no
		is_at_war = no
		is_incapable_trigger = no
	}

	ai_will_do = {
		base = 100
	}
}
