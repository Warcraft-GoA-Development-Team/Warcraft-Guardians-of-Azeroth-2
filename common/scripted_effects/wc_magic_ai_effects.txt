decide_to_cast_spell_effect = { # Stage 2
    random_list = {
        50 = { # Yes
            change_global_variable = {
                name = ai_spells_pre_cast_stage_2
                add = 1
            }
        }
        50 = { # Nahh
            

            # Come back later
            trigger_event = {
                on_action = wc_on_ai_spellcasting
                days = { 7 35 } # So it will not cause a major lag on every 1st of each month
            }
        }
    }
}