
	is_shown = {
		debug_only = yes
	}

	is_valid = {
	}
	effect = {
		root = {
			if = {
				limit = {
					NOT = {
						has_variable = light_void_infusion
					}
				}
				set_variable = {
					name = light_void_infusion
					value = 0
				}
			}
			change_variable = {
				name = light_void_infusion
				add = 20
			}
		}
	}

	ai_check_interval = 0
}
