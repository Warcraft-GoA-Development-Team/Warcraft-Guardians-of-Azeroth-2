restore_empire_of_zul_decision = {
	ai_check_interval = 120
	major = yes
	desc = restore_empire_of_zul_decision_desc
	selection_tooltip = restore_empire_of_zul_decision_tt

	is_shown = {
		culture = { has_cultural_pillar = heritage_zulite }
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:restore_empire_of_zul_decision
			}
		}
		OR = {
			title:e_amani.holder ?= root
			title:e_drakkari.holder ?= root
			title:e_gurubashi.holder ?= root
			title:e_zandalari.holder ?= root
			title:e_zulfarrak.holder ?= root
		}
	}

	cost = {					
		gold = 750
		prestige = 1000
	}
	
	is_valid = {
		realm_size >= 30
		prestige_level >= 4
		calc_true_if = {
			amount >= 2
			title:e_amani.holder ?= root
			title:e_drakkari.holder ?= root
			title:e_gurubashi.holder ?= root
			title:e_zandalari.holder ?= root
			title:e_zulfarrak.holder ?= root
		}
		calc_true_if = {
			amount >= 3
			title:d_gundrak.holder ?= root			
			title:d_drakmabwa.holder ?= root
			title:d_hebdrakkar.holder ?= root
			title:d_draktharon.holder ?= root
			title:d_draksotra.holder ?= root
			title:d_jagtar.holder ?= root
			title:d_zuldazar.holder ?= root
			title:d_mugamba.holder ?= root
			title:d_mugamba.holder ?= root
			title:d_nazmir.holder ?= root
			title:d_torwatha.holder ?= root
			title:d_zulaman.holder ?= root
			title:d_jinthaalor.holder ?= root
			title:d_zulgurub.holder ?= root
			title:d_zulkunda.holder ?= root
		}
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_capable_adult = yes
		is_at_war = no
		is_independent_ruler = yes
	}

	effect = {
		# Can only happen once
		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:restore_empire_of_zul_decision
			}
		}
		custom_tooltip = restore_empire_of_zul_decision_tt
		save_scope_as = founder

		create_empire_of_zul_effect = yes

		# Notification
		every_player = {
			limit = {
				OR = {
					culture = { has_cultural_pillar = heritage_zulite }
					is_within_diplo_range = { CHARACTER = scope:founder }
				}
			}
			trigger_event = zul_restoration.0001
		}
		
		add_prestige_level = 2
		add_trait = augustus
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}