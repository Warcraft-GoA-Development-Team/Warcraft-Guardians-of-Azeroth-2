borrow_money_decision = {
	title = borrow_money_title
	picture = "gfx/interface/illustrations/decisions/borrowmoney.dds"
	ai_goal = no
	ai_check_interval = 0
	selection_tooltip = borrow_money_tooltip
	desc = borrow_money_decision_desc
	confirm_text = borrow_money_decision_confirm

	is_shown = {
		NOT = {
			has_trait = owes_money
			has_character_flag = borrowedmoney
			has_character_flag = paidloan
		}
	}

	cost = {
		prestige = 100
                piety= 50
	}

	effect = {
		add_gold = 500
		add_stress = minor_stress_gain
		add_trait = money_lender
		# trigger_event=pb.1
		add_character_flag = { flag = loanperiod years = 2 }
		add_character_flag = { flag = borrowedmoney }
	}

	ai_potential = {
		always = yes
	}
	ai_will_do = {
		base = 5
	}

}

pay_money_decision = {
	title = pay_money_title			# override title, default: "<key>"; supports dynamic descriptions like in events (first_valid, ...); scope: character
	picture = "gfx/interface/illustrations/decisions/paymoney.dds"
	ai_goal = no
	ai_check_interval = 0
	selection_tooltip = pay_money_tooltip
	desc = pay_money_decision_desc
	confirm_text = pay_money_decision_confirm

	is_shown = {
		OR = {
			has_trait = owes_money
			has_character_flag = borrowedmoney
		}
	}
	

	cost = {
		gold = 570
	}

	effect = {
		remove_character_flag = loanperiod
		remove_character_flag = borrowedmoney
		add_character_flag = { flag = paidloan years = 5 }
		remove_trait = owes_money
		remove_trait = money_lender
		add_prestige = 20
                add_piety = 20
	}

	ai_potential = {
		always = yes
	}
	ai_will_do = {
		base = 4
	}

}
