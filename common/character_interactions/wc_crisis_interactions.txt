join_coalition_interaction = {
	interface_priority = 60
	category = interaction_category_diplomacy
	desc = invite_to_pol_faction_interaction_desc

	is_shown = {
		scope:actor = {
			can_be_in_coalition_trigger = yes
			NOT = { is_target_in_variable_list = { name = coalition_targets target = scope:recipient } }
		}
		scope:recipient = {
			is_crisis_trigger = yes
		}
	}
	
	is_valid_showing_failures_only = {
		scope:actor = {
			is_at_war = no
		}
	}
	
	on_accept = {
		scope:actor = {
			join_crisis_coalition_effect = { CRISIS_HOLDER = scope:recipient }
		}
	}
	auto_accept = yes

	ai_potential = {
		always = yes
	}
	ai_targets = {
		ai_recipients = neighboring_rulers
	}
	
	ai_frequency = 12
	ai_will_do = {
		base = 0
		modifier = {
			add = 100
			ai_has_coalition_reason_trigger = { CRISIS_HOLDER = scope:recipient }
		}
	}
}
leave_coalition_interaction = {
	interface_priority = 60
	category = interaction_category_diplomacy
	desc = invite_to_pol_faction_interaction_desc

	is_shown = {
		scope:actor = {
			is_target_in_variable_list = { name = coalition_targets target = scope:recipient }
		}
	}
	
	is_valid_showing_failures_only = {
		scope:actor = {
			is_at_war = no
		}
	}
	
	on_accept = {
		scope:actor = {
			leave_crisis_coalition_effect = { CRISIS_HOLDER = scope:recipient }
		}
	}
	auto_accept = yes

	ai_potential = {
		always = yes
	}
	ai_targets = {
		ai_recipients = neighboring_rulers
	}
	
	ai_frequency = 12
	ai_will_do = {
		base = 0
		modifier = {
			add = 100
			NOT = { ai_has_coalition_reason_trigger = { CRISIS_HOLDER = scope:recipient } }
		}
	}
}