age_in_relation_to_humans_more_than_trigger = {
	save_temporary_scope_value_as = {
		name = temp_age
		value = {
			value = $age$
			if = {
				limit = { $age$ > 20 }
				multiply = racial_age_multiplier_value
			}
		}
	}
	
	age > scope:temp_age
}

age_in_relation_to_humans_equal_trigger = {
	save_temporary_scope_value_as = {
		name = temp_age
		value = {
			value = $age$
			if = {
				limit = { $age$ > 20 }
				multiply = racial_age_multiplier_value
			}
		}
	}
	
	age = scope:temp_age
}

age_in_relation_to_humans_less_than_trigger = {
	save_temporary_scope_value_as = {
		name = temp_age
		value = {
			value = $age$
			if = {
				limit = { $age$ > 20 }
				multiply = racial_age_multiplier_value
			}
		}
	}
	
	age < scope:temp_age
}

age_in_relation_to_humans_range_trigger = {
	save_temporary_scope_value_as = {
		name = temp_min_age
		value = {
			value = $min_age$
			if = {
				limit = { $min_age$ > 20 }
				multiply = racial_age_multiplier_value
			}
		}
	}
	save_temporary_scope_value_as = {
		name = temp_max_age
		value = {
			value = $max_age$
			if = {
				limit = { $max_age$ > 20 }
				multiply = racial_age_multiplier_value
			}
		}
	}
	
	age >= scope:temp_min_age
	age <= scope:temp_max_age
}

is_child_trigger = {
	age < age_12_value
}
is_teen_trigger = {
	is_child_trigger = no
	is_adult = no
}
race_relative_age_between_trigger = {
	age >= { value = scope:my_race_age_multiplier multiply = $AGE_1$ }
	age < { value = scope:my_race_age_multiplier multiply = $AGE_2$ }
}
no_aging_penalty_trigger = {
	NOR = {
		has_character_modifier = $AGE$_age_penalty_modifier
		has_character_modifier = $AGE$_age_penalty_without_prowess_modifier
	}
}


relative_age_floor_for_VALUE_greater_than_15 = { 
	save_temporary_scope_value_as = { 
		name = temp_floor_age
		value = 16
		subtract = { 
			value = $VALUE$
			multiply = racial_age_multiplier_value
		}
		min = 0
	}
	scope:temp_floor_age >= 16
}