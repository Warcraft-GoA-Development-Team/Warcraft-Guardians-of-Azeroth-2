crisis_quarterly_pulse = {
    random_events = {
		chance_of_no_event = {
			value = 50
		}
		
		100 = wc_crisis_events.0010 # Coalition members join wars
        50 = wc_horde_invasion.0005 # Horde Invasion troops
        100 = wc_crisis_events.0200 # The coalition against you is large
        50 = wc_crisis_events.0210 # Attempt to turn a coalition member to your side
	}
    effect = {
        # Start plague of andorhal in 603
        if = {
            limit = {
                exists = title:e_scourge.holder
                dark_portal_is_opened_trigger = no 
                game_start_date < 603.1.1
                current_date >= 603.1.1 
            }
            title:e_scourge.holder = { find_scourge_story_effect = yes }
            scope:story = { scourge_603_plague_effect = yes }
        }
        if = { 
            limit = {   
                NOT = { is_target_in_global_variable_list = { name = unavailable_unique_events target = flag:beyond_dark_portal } }
                current_date >= 600.1.1
            }
            every_ruler = {
                limit = {
                    is_ai = yes 
                    is_landless_adventurer = yes 
                }
                ai_attempt_to_host_activity = activity_beyond_dark_portal
            }
        }
    }
}