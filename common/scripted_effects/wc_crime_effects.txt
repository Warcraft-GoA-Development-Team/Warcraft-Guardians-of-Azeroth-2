wc_assign_gallowsbait_if_needed = { 
	if = {
		limit = {
			is_landless_adventurer = yes
			has_trait_with_flag = human_elf_dwarf_crime
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait_with_flag = human_elf_dwarf_crime  # Human, Elves and Dwarves do not tolerate crimes against each other
			}
		}
		if = {
			limit = { scope:recipient.has_trait_with_flag = human_elf_dwarf_crime }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_massive_gain
				XP_MIN = gallowsbait_xp_massive_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait_with_flag = horde_crime
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait_with_flag = horde_crime  # Horde factions do not tolerate crimes against each other
			}
		}
		if = {
			limit = { scope:recipient.has_trait_with_flag = horde_crime }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_massive_gain
				XP_MIN = gallowsbait_xp_massive_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait_with_flag = dragon_crime  # Dragons needlessly commiting crimes against each other is not allowed
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait_with_flag = dragon_crime
			}
		}
		if = {
			limit = { scope:recipient.has_trait_with_flag = dragon_crime }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_major_gain
				XP_MIN = gallowsbait_xp_major_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait_with_flag = vrykul_family
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait_with_flag = vrykul_family # Seems to be a viking-like coherant society
			}
		}
		if = {
			limit = { scope:recipient.has_trait_with_flag = vrykul_family }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_medium_gain
				XP_MIN = gallowsbait_xp_medium_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait = creature_tuskarr
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait = creature_tuskarr  # Tuskarr's seem to be a coherant society
			}
		}
		if = {
			limit = { scope:recipient.has_trait = creature_tuskarr }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_medium_gain
				XP_MIN = gallowsbait_xp_medium_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait = creature_taunka
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait = creature_taunka  # Taunka seem to be a coherant society
			}
		}
		if = {
			limit = { scope:recipient.has_trait = creature_taunka }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_medium_gain
				XP_MIN = gallowsbait_xp_medium_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait_with_flag = titanforged_class
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait_with_flag = titanforged_class
			}
		}
		if = {
			limit = { scope:recipient.has_trait_with_flag = titanforged_class }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_medium_gain
				XP_MIN = gallowsbait_xp_medium_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait_with_flag = nature_crime
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait_with_flag = nature_crime
			}
		}
		if = {
			limit = { scope:recipient.has_trait_with_flag = nature_crime }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_medium_gain
				XP_MIN = gallowsbait_xp_medium_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait = creature_tortollan
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait = creature_tortollan  # Tortollans also seem to be a peacuful and coherant society
			}
		}
		if = {
			limit = { scope:recipient.has_trait = creature_tortollan }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_medium_gain
				XP_MIN = gallowsbait_xp_medium_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait = creature_mogu
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait = creature_mogu
			}
		}
		if = {
			limit = { scope:recipient.has_trait = creature_mogu }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_massive_gain
				XP_MIN = gallowsbait_xp_massive_gain
			}
		}
	}
	else_if = {
		limit = {
			is_landless_adventurer = yes
			has_trait = creature_mogu
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait = thunder_king
			}
		}
		if = {
			limit = { scope:recipient.has_trait = thunder_king }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_major_gain
				XP_MIN = gallowsbait_xp_major_gain
			}
		}
	}
	else = {
		limit = {
			is_landless_adventurer = yes
			has_trait = being_undead
			scope:recipient = {
				is_landless_adventurer = no
				is_ruler = yes
				has_trait = lich_king
			}
		}
		if = {
			limit = { scope:recipient.has_trait = lich_king }
			laamp_rewards_apply_criminal_xp_effect = {
				TRACK = marauder
				XP_MAX = gallowsbait_xp_major_gain
				XP_MIN = gallowsbait_xp_major_gain
			}
		}
	}
}