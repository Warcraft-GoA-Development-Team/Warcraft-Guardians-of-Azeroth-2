build_cosmic_observatory_decision = { 
	picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" } #TODO
	decision_group_type = major

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 36
		kingdom = 36
		empire = 36
		hegemony = 36
	}

	desc = build_cosmic_observatory_decision_desc

	is_shown = {
		culture = { has_cultural_pillar = heritage_thalassian }
		is_landed = yes
		highest_held_title_tier >= tier_duchy
		OR = { # Show it earlier so it's something to work towards
			has_trait = lifestyle_mystic
			has_trait = scholar
			has_education_learning_trigger = yes
		}
		NOR = {
			top_liege = { 
				any_sub_realm_county = {
					has_variable = has_cosmic_observatory
				}
			}
			has_trait = demon_hunter # Demon Hunters shouldn't become void being. Canonically not a thing.
		}
		
		# MAGIC 2024 TODO: void magic pathway
	}

	is_valid = {
		has_trait = lifestyle_mystic
		OR = { 
			has_trait = education_learning_5
			AND = { 
				OR = { 
					has_trait = education_learning_3
					has_trait = education_learning_4
				}
				has_trait = scholar
			}
			# MAGIC 2024 TODO: void magic pathway
		}
	}

	is_valid_showing_failures_only = { 
		custom_tooltip = {
			text = cant_be_hof
			NOT = {
				any_held_title = {
					is_head_of_faith = yes
				}
			}
		}
	}

	widget = {
		controller = create_holy_order
		gui = "decision_view_widget_place_special_building"
		barony_valid = {
			OR = {
				holder = scope:ruler
				AND = { 
					holder.primary_title.tier = tier_barony
					exists = holder.liege
					holder.liege = scope:ruler
				}
			}
			title_province = {
				has_special_building = no
				has_special_building_slot = no
			}
		}
	}

	effect = {
		scope:barony ?= {
			title_province = {
				add_special_building_slot = cosmic_observatory
			}
			county = {
				set_variable = has_cosmic_observatory
			}
		}
		
		add_character_flag = can_build_observatory
	}
}

restore_well_of_eternity = {
	picture = { reference = "gfx/interface/illustrations/decisions/restore_well_of_eternity.dds" }
	decision_group_type = major

	ai_check_interval_by_tier = {
		barony = 0
		county = 15
		duchy = 12
		kingdom = 9
		empire = 6
		hegemony = 3
	}

	desc = restore_well_of_eternity_desc

	is_shown = {
		OR = {
			has_trait = creature_night_elf
			has_trait = creature_dryad
		}
		is_independent_ruler = yes
		title:b_nordrassil.title_province = {
			OR = {
				has_building = nordrassil_destroyed
				has_building = nordrassil_sacrificed
			}
		}
	}

	is_valid = {
		NOT = { has_religion = religion:doombringer } # You serve Sargeras, what are you doing?
		OR = {
			has_trait = creature_night_elf
			has_trait = creature_dryad
		}
		is_independent_ruler = yes
		completely_controls = title:c_nordrassil
		completely_controls = title:c_darnassus
	}

	cost = {
		gold = 1000
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_at_war = no
	}

	effect = {
		custom_tooltip = {
			text = restore_well_of_eternity_tt
			trigger_event = {
				id = restorationofnordassil.0001
				years = 2
			}
		}
	}

	ai_potential = {
		is_imprisoned = no
		is_at_war = no
	}

	ai_will_do = {
		base = 100
	}
}
