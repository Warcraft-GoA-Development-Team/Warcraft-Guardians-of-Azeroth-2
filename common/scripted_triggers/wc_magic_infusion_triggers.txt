has_infusion_amount_trigger = {
	trigger_if = {
		limit = {
			has_variable = $MAGIC$_infusion
		}
		
		OR = {
			AND = {
				$AMOUNT$ > 0
				var:$MAGIC$_infusion >= $AMOUNT$
			}

			AND = {
				$AMOUNT$ < 0
				var:$MAGIC$_infusion <= $AMOUNT$
			}
		}
	}
	trigger_else = {
		always = no
	}
}

has_infusion_level_trigger = {
	OR = {
		AND = {
			$AMOUNT$ > 0
			$MAGIC$_infusion_level_value >= $AMOUNT$
		}

		AND = {
			$AMOUNT$ < 0
			$MAGIC$_infusion_level_value <= $AMOUNT$
		}
	}
}

has_infusion_trait = {
	OR = {
		has_trait = being_light
		has_trait = being_void
		has_trait = being_order
		has_trait = being_demon
		has_trait = being_life
		has_trait = being_undead
	}
}

has_infusion_flag = {
	OR = {
		has_character_flag = demon_ritual
	}
}