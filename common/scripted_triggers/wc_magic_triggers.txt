has_collected_souls_amount_trigger = {
	trigger_if = {
		limit = {
			has_variable = collected_souls
		}
		
		OR = {
			AND = {
				$AMOUNT$ > 0
				var:collected_souls >= $AMOUNT$
			}
		}
	}
	trigger_else = {
		always = no
	}
}