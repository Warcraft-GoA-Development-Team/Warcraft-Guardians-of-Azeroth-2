visit_the_arcandor_decision = {
    # By Dione
    ai_check_interval = 45
	picture = {
		reference = "gfx/interface/illustrations/decisions/restore_arcandor_decision.dds"
	}
	decision_group_type = elven

    is_shown = {
        has_global_variable = arcandor_restored_global_variable
        OR = {
            has_trait = wretched
            has_trait = creature_faldorei
        }
        NOT = { has_character_flag = visited_arcandor_tree }
    }

    cost = {
        prestige = major_prestige_value
    }

    is_valid_showing_failures_only = {
		is_available_adult = yes
		is_at_war = no
	}

    effect = {
        custom_tooltip = visit_the_arcandor_decision_effect_tt
        start_travel_plan = {
            destination = title:b_meredil.title_province
            travel_with_domicile = yes
            on_arrival_event = wc_suramar_events.1002
        }
        if = {
            limit = {
                has_trait = wretched
            }
            show_as_tooltip = { remove_trait = wretched }
        }
    }

    ai_potential = {
		is_available_adult = yes
	}

	ai_will_do = {
		base = 100
	}
}