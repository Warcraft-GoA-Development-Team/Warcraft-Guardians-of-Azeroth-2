wounded_chance_modifier = {
	modifier = {
		add = 1
	}

	modifier = {
		factor = { # Prowess directly reduces chance of injury/death
			value = $PROWESS_DIVIDE$ # Value at which 'immortality' happens
			subtract = prowess
			divide = $PROWESS_DIVIDE$
			min = 0.1 # Everyone dies.
		}
	}

	modifier = {
		factor = 2
		has_trait = brave
	}
	modifier = {
		factor = 0.5
		has_trait = craven
	}
}