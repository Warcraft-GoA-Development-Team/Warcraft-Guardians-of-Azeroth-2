namespace = train_demon_hunter

#### train_demon_hunter.success -> Event for train_demon_hunter scheme success
#### train_demon_hunter.failure -> Event for train_demon_hunter scheme failure

train_demon_hunter.success = {
    type = character_event
    content_source = dlc_GOA
    title = train_demon_hunter_success_title
    desc = train_demon_hunter_success_desc
    theme = corruption

    override_background = {
        reference = wc_background_tomb_of_sargeras
    }

    left_portrait = {
        character = scope:owner
        animation = prayer
    }

    right_portrait = {
        character = scope:target
        animation = wedding_priest
    }

    option = {
        name = train_demon_hunter_success_A
        scope:owner = {
			send_interface_toast = {
				title = train_demon_hunter_complete
				left_icon = scope:target
				scope:scheme = {
					end_scheme = yes
				}
			}
            add_trait = { demon_hunter }
            add_character_flag = became_demon_hunter_by_being_a_novice
		}
    }
}

train_demon_hunter.failure = {
    type = character_event
    content_source = dlc_GOA
    title = train_demon_hunter_failure_title
    desc = train_demon_hunter_failure_desc
    theme = corruption

    override_background = {
        reference = wc_background_tomb_of_sargeras
    }

    left_portrait = {
        character = scope:owner
        animation = loss_1
    }

    right_portrait = {
        character = scope:target
        animation = disgust
    }

    option = {
        name = train_demon_hunter_failure_A
        scope:owner = {
			send_interface_toast = {
				title = train_demon_hunter_complete
				left_icon = scope:target
				scope:scheme = {
					end_scheme = yes
				}
			}
		}
    }
}
