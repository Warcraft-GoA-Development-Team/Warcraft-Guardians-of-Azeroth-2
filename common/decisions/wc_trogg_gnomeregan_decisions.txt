invade_gnomeregan_decision = {
	title = {
		first_valid = {
			desc = invade_gnomeregan_decision
		}
	}
	desc = {
		first_valid = {
			desc = invade_gnomeregan_decision.desc
		}
	}
	
	ai_check_interval = 12
	decision_group_type = major
	picture = { reference = "gfx/interface/illustrations/decisions/gnomeregan.dds" }
	selection_tooltip = invade_gnomeregan_decision_tt

	is_shown = {
		ROOT = {
			has_trait = creature_trogg
			is_landed = yes
		}

		if = {
			limit = {
				is_ai = yes
			}
			current_year > 603
		}
		else = {
			current_year > 602
		}

		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:invade_gnomeregan_decision
			}
		}

		game_start_date < 604.9.15
	}
	

	cost = {
		gold = 10
		prestige = 100
	}
	
	is_valid = {
		title:k_gnomeregan.holder = {
			has_trait = creature_gnome
		}
	}

	effect = {
		custom_tooltip = invade_gnomeregan_decision_effect.tt

		trigger_event = trogg_invasion.0001

		hidden_effect = {
			add_to_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:invade_gnomeregan_decision
			}
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100

		modifier = {
			add = -50
			title:k_gnomeregan.holder = {
				is_allied_to = title:k_ironforge.holder
			}
		}

		modifier = {
			add = 10
			title:k_gnomeregan.holder = {
				is_at_war = yes
			}
		}

		modifier = {
			add = 75
			title:k_gnomeregan.holder = {
				OR = {
					is_at_war_with = title:k_lordaeron.holder
					is_at_war_with = title:k_lordaeron.holder.liege
				}
			}
		}
	}
}

spread_radiation_decision = {
	title = {
		first_valid = {
			desc = spread_radiation_decision
		}
	}
	desc = {
		first_valid = {
			desc = spread_radiation_decision.desc
		}
	}
	
	ai_check_interval = 12
	decision_group_type = major
	picture = { reference = "gfx/interface/illustrations/decisions/gnomeregan.dds" }
	selection_tooltip = spread_radiation_decision_tt

	is_shown = {
		has_trait = creature_gnome
		has_trait = irradiated
		is_landed = yes
	}
	
	is_valid = {
		OR = {
			title:c_gnomeregan.holder = ROOT
			title:c_gnomeregan.holder = ROOT.liege
		}
	}
	
	effect = {
		custom_tooltip = spread_radiation_decision_effect.tt

		hidden_effect = {
			every_courtier_or_guest = {
				limit = {
					NOT = {
						has_trait = being_undead
					}
				}
				add_trait = irradiated
				set_culture = culture:leper_gnome
				set_character_faith = faith:glowbound
			}

			every_prisoner = {
				limit = {
					NOT = {
						has_trait = being_undead
					}
				}
				add_trait = irradiated
				set_culture = culture:leper_gnome
				set_character_faith = faith:glowbound		
			}

			if = {
				limit = {
					any_realm_county = {
						OR = {
							has_county_modifier = wc_high_debris_damage_modifier
							has_county_modifier = wc_medium_debris_damage_modifier
							has_county_modifier = wc_small_debris_damage_modifier 
						}				
					}
				}

				every_realm_county = {
					remove_all_county_modifier_instances = wc_high_debris_damage_modifier
					remove_all_county_modifier_instances = wc_medium_debris_damage_modifier
					remove_all_county_modifier_instances = wc_small_debris_damage_modifier
				}
			}
		}
	}


	cost = {
		prestige = 30
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}

	cooldown = { 
		years = 3
	}
}

gnomeregan_rebuilding_efforts_decision = {
	title = {
		first_valid = {
			desc = gnomeregan_rebuilding_efforts_decision
		}
	}
	desc = {
		first_valid = {
			desc = gnomeregan_rebuilding_efforts_decision.desc
		}
	}
	
	ai_check_interval = 12
	decision_group_type = major
	picture = { reference = "gfx/interface/illustrations/decisions/decontamination_gnomeregan.dds" }
	selection_tooltip = gnomeregan_rebuilding_efforts_decision_tt

	is_shown = {
		NOT = {
			ROOT = {
				has_trait = irradiated
			}
		}
		
		title:k_gnomeregan.holder = ROOT


		ROOT = {
			any_realm_county = {
				OR = {
					has_county_modifier = wc_high_debris_damage_modifier
					has_county_modifier = wc_medium_debris_damage_modifier
					has_county_modifier = wc_small_debris_damage_modifier
				}
			}
		}
	}
	
	effect = {
		custom_tooltip = gnomeregan_rebuilding_efforts_decision_effect.tt
		ROOT = {
			trigger_event = trogg_invasion.0010
		}
	}


	cost = {

	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 70
	}

	cooldown = { 
		years = 1
	}
}

operation_gnomeregan_decision = {
	title = {
		first_valid = {
			desc = operation_gnomeregan_decision
		}
	}
	desc = {
		first_valid = {
			desc = operation_gnomeregan_decision.desc
		}
	}
	
	ai_check_interval = 12
	decision_group_type = major
	picture = { reference = "gfx/interface/illustrations/decisions/gnomeregan.dds" }
	selection_tooltip = operation_gnomeregan_decision_tt

	is_shown = {
		is_at_war = no

		AND = {
			exists = title:d_gnomeregan.holder
			exists = title:k_gnomeregan.holder

			title:d_gnomeregan.holder = {
				has_trait = irradiated
			}

			ROOT = title:k_gnomeregan.holder

			title:c_icewind_valley = {
				NOT = {
					OR = {
						has_county_modifier = wc_high_debris_damage_modifier
						has_county_modifier = wc_medium_debris_damage_modifier
						has_county_modifier = wc_small_debris_damage_modifier 					
					}
				}
			}

			title:c_icewind_pass = {
				NOT = {
					OR = {
						has_county_modifier = wc_high_debris_damage_modifier
						has_county_modifier = wc_medium_debris_damage_modifier
						has_county_modifier = wc_small_debris_damage_modifier 					
					}
				}
			}

			title:c_new_tinkertown = {
				NOT = {
					OR = {
						has_county_modifier = wc_high_debris_damage_modifier
						has_county_modifier = wc_medium_debris_damage_modifier
						has_county_modifier = wc_small_debris_damage_modifier 					
					}
				}
			}		
		}
		ROOT = {
			has_trait = creature_gnome
			
			NOT = {
				has_truce = title:d_gnomeregan.holder
			}	
		}

		title:k_gnomeregan.holder = ROOT
		
		OR = {
			game_start_date > 604.9.15

			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:invade_gnomeregan_decision
			}
		}

		if = {
			limit = {
				is_ai = yes
			}

			current_year > 609
		}
	}
	

	cost = {
		gold = 100
		prestige = 20
	}

	effect = {
		custom_tooltip = operation_gnomeregan_decision_effect.tt

		start_war = {
			casus_belli = operation_gnomeregan_cb
			target = title:d_gnomeregan.holder
			target_title = title:d_gnomeregan
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 100
	}
}
