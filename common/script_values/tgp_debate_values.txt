debate_activity_base_cost = {
	value = 0
	add = standard_playdate_activity_cost
}

debate_predicted_cost = {
	value = 0
	add = {
		# Base Cost
		value = {
			value = debate_activity_base_cost
			desc = debate_activity_base_cost
		}
		# Scale by Era
		value = {
			value = debate_activity_base_cost
			multiply = activity_cost_scale_by_era
			subtract = debate_activity_base_cost
			desc = activity_cost_scale_by_era_desc
		}
		# Kaifeng cost bonus
		# WARCRAFT
		# if = {
		# 	limit = {
		# 		root.capital_province = { has_building_or_higher = kaifeng_palace_01 }
		# 	}
		# 	multiply = {
		# 		value = {
		# 			value = 1
		# 			subtract = kaifeng_examination_cost_value
		# 		}
		# 		desc = kaifeng_palace_name_desc
		# 	}
		# }

		# Options 
		add = { # Books Option 
			value = 0
			subtract = minor_gold_value_static_max
			add = {
				value = medium_gold_value_static_max
				min = 150
			}
			add = {
				value = major_gold_value_static_max
				min = 300
			}
			# Scale option costs by era
			multiply = activity_cost_scale_by_era
			# Divide by the number of option levels to get an average option cost
			divide = 4
		}
	}
}

tgp_debate_success_score_value = {
	value = {
		add = learning
		add = diplomacy
		add = martial
		every_relation = {
			type = disciple
			add = {
				add = learning
				add = diplomacy
				add = martial
				multiply = 0.5
			}
		}
		add = {
			if = {
				limit = {
					has_trait = confucian_education
					has_trait_xp = {
						trait = confucian_education
						value > 70
					}
				}
				add = 20
			}
			else_if = {
				limit = {
					has_trait = confucian_education
					has_trait_xp = {
						trait = confucian_education
						value > 50
					}
				}
				add = 15
			}
			else_if = {
				limit = {
					has_trait = confucian_education
					has_trait_xp = {
						trait = confucian_education
						value > 20
					}
				}
				add = 10
			}
			else_if = {
				limit = { has_trait = confucian_education }
				add = 5
			}
		}
		add = {
			if = {
				limit = {
					this = scope:host
					scope:activity = {
						has_activity_option = {
							category = debate_option_books
							option = debate_books_normal
						}
					}
				}
				add = 15
			}
		}
		add = {
			if = {
				limit = {
					this = scope:host
					scope:activity = {
						has_activity_option = {
							category = debate_option_books
							option = debate_books_good
						}
					}
				}
				add = 30
			}
		}
	}
}

debate_guest_influence_reward_value = {
	value = 30
	add = var:tgp_debate_success_score
	multiply = 0.6
}

debate_host_prestige_reward_value = {
	value = 30
	add = var:tgp_debate_success_score
	multiply = 0.5
}