macro = {
    description = ""
    definition = "HiddenLandsCustom(CustomOne,CustomTwo)"
    replace_with = "Select_CString(GetPlayer.IsValid, CustomOne, CustomTwo)"
}

macro = {
    description = ""
    definition = "HiddenLandsPlayer(SGUI,Target,Method)"
    replace_with = "Select_CString(GetScriptedGui(SGUI).IsValid( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope('target', Target.MakeScope).End), Method, Localize('MAP_DATA_UNKNOWN'))"
}

macro = {
    description = ""
    definition = "HiddenLandsPlayerEpidemic(SGUI,Target,Method)"
    replace_with = "Select_CString(GetScriptedGui(SGUI).IsValid( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope('target', Target.MakeScope).End), Method, province.Custom('EpidemicUnknown'))"
}