build_cosmic_observatory_decision = { 
	picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" } #TODO
	decision_group_type = major

	ai_check_interval = 36

	desc = build_cosmic_observatory_decision_desc

	is_shown = {
		culture = { has_cultural_pillar = heritage_highborne }
		is_landed = yes
		highest_held_title_tier >= tier_duchy
		OR = { # Show it earlier so it's something to work towards
			has_trait = lifestyle_mystic
			has_trait = scholar
			has_education_learning_trigger = yes
		}
		NOR = {
			top_liege = { 
				any_sub_realm_county = {
					has_variable = has_cosmic_observatory
				}
			}
			has_trait = demon_hunter # Demon Hunters shouldn't become void being. Canonically not a thing.
		}
		
		# MAGIC 2024 TODO: void magic pathway
	}

	is_valid = {
		has_trait = lifestyle_mystic
		OR = { 
			has_trait = education_learning_5
			AND = { 
				OR = { 
					has_trait = education_learning_3
					has_trait = education_learning_4
				}
				has_trait = scholar
			}
			# MAGIC 2024 TODO: void magic pathway
		}
	}

	is_valid_showing_failures_only = { 
		custom_tooltip = {
			text = cant_be_hof
			NOT = {
				any_held_title = {
					is_head_of_faith = yes
				}
			}
		}
	}

	widget = {
		controller = create_holy_order
		gui = "decision_view_widget_place_special_building"
		barony_valid = {
			OR = {
				holder = scope:ruler
				AND = { 
					holder.primary_title.tier = tier_barony
					exists = holder.liege
					holder.liege = scope:ruler
				}
			}
			title_province = {
				has_special_building = no
				has_special_building_slot = no
			}
		}
	}

	effect = {
		scope:barony ?= {
			title_province = {
				add_special_building_slot = cosmic_observatory
			}
			county = {
				set_variable = has_cosmic_observatory
			}
		}
		
		add_character_flag = can_build_observatory
	}
}

become_a_demon_hunter_decision = { # By Ikarimonogatari
	picture = { reference = "gfx/interface/illustrations/decisions/becoming_a_demon_hunter.dds" }
	decision_group_type = major

	ai_check_interval = 60

	desc = become_a_demon_hunter_decision_desc

	is_shown = {
		OR = {
			has_trait = creature_night_elf
			has_trait = creature_high_elf
		}
		NOR = {
			has_trait = demon_hunter
			has_trait = being_void # Because of event: void_elf.0130
			has_trait = being_undead # Darkfallen Elves never became Demon Hunters

			# Historically, these characters shouln't be allowed to become demon hunters
			is_tyrande_trigger = yes # Tyrande
			is_shandris_trigger = yes # Shandris
			is_malfurion_trigger = yes # Malfurion
			is_maiev_trigger = yes # Maiev Shadowsong
		}
	}

	is_valid = {
		custom_tooltip = {
			text = a_demon_is_imprisoned_by_you
			any_prisoner = {
				has_trait = being_demon
				NOT = { has_trait = demon_hunter }
			}
		}
		OR = {
			has_trait = creature_night_elf
			has_trait = creature_high_elf	
		}
		OR = { # Only certain personalities can take such a drastic decision
			has_trait = vengeful
			has_trait = callous
			has_trait = cynical # Since you don't trust your old religion to defeat the demons
			has_trait = wrathful
		}
		NOR = {
			has_trait = forgiving
			has_trait = compassionate
			has_trait = trusting
			has_trait = loyal
			has_trait = zealous # Why would you abandon your old religion so easily?
			has_trait = content
			has_trait = craven
			has_trait = calm
		}
	}

	cost = {
		piety = 2000
        prestige = 2000
    }

	is_valid_showing_failures_only = {
		is_at_war = no
	}

	effect = {
		add_trait = demon_hunter
		add_trait = blind        # Should instead be an event, when the Demon Hunter trait track reaches XP = 66
		add_trait = being_demon  # Should instead be an event, when the Demon Hunter trait track reaches XP = 100
		show_as_tooltip = { set_character_faith = faith:illidari }
		hidden_effect = {
			set_character_faith_with_conversion = faith:illidari
		}
		add_character_flag = should_be_topless # Most of them seem to be topless after they become Demon Hunters
	}

	ai_potential = {
		is_at_war = no
	}

	ai_will_do = {
		base = 5
		modifier = {
			add = 95
			has_faith = faith:illidari # If you are already a Vindicatory faith elf but haven't became a Demon Hunter yet
		}
		modifier = {
			add = 10
			current_date >= 618.1.1 # Lore-wise, this is when Demon Hunters canonically start appearing
		}
	}
}

restore_the_queldorei_rule_decision = { # By Ikarimonogatari
	picture = { reference = "gfx/interface/illustrations/decisions/restore_the_highborne_to_glory.dds" }
	decision_group_type = major

	ai_check_interval = 12

	desc = restore_the_queldorei_rule_decision_desc

	is_shown = {
		is_independent_ruler = yes
		has_trait = creature_highborne
		has_culture = culture:highborne
		NOT = { has_character_flag = has_taken_decision_to_restore_queldorei_rule }
	}

	is_valid = {
		is_independent_ruler = yes # To avoid vassals taking the decision after a liege has already taken it
		has_trait = creature_highborne
		title:c_nordrassil = {
			holder = root
		}
		prestige_level >= 4
		piety_level >= 4
	}

	cost = {
		prestige = 4000
		piety = 4000
		gold = 1000
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
		is_imprisoned = no
		is_at_war = no
	}

	effect = {
		custom_tooltip = restore_the_queldorei_rule_decision_tt
		trigger_event = {
			id = wc_restoring_the_queldorei.0001
		}
		add_character_flag = has_taken_decision_to_restore_queldorei_rule
	}

	ai_potential = {
		is_available_adult = yes
		is_imprisoned = no
		is_at_war = no
	}

	ai_will_do = {
		base = 100
	}
}

reintegrate_shendralar_into_kaldorei_decision = { # By Ikarimonogatari, as Requested in Suggestion #289
	picture = { reference = "gfx/interface/illustrations/decisions/reintegrating_shendralar.dds" }
	decision_group_type = major

	desc = reintegrate_shendralar_into_kaldorei_decision_desc

	ai_check_interval = 12

	is_shown = {
		has_trait = creature_night_elf
		title:e_kaldorei.holder = root
		NOT = { has_character_flag = has_mended_the_night_elf_highborne_schism }
	}

	is_valid = {
		has_trait = creature_night_elf
		culture = { has_cultural_pillar = heritage_darnassian }
		title:e_kaldorei.holder = root
		completely_controls = title:k_eldrethalas
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_at_war = no
	}

	cost = {
		gold = 200
		prestige = 2000
	}

	effect = {
		add_legitimacy = 200
		title:k_eldrethalas = {
			set_de_jure_liege_title = title:e_kaldorei
		}
		add_character_flag = has_mended_the_night_elf_highborne_schism
		culture:night_elf = {
			change_cultural_acceptance = {
				target = culture:highborne
				value = 100
				desc = shendralar_and_kaldorei_cultures_are_united_desc
			}
		}
	}

	ai_potential = {
		is_imprisoned = no
		is_at_war = no
	}

	ai_will_do = {
		base = 50
	}
}

legacy_of_defias_brotherhood_decision = { # By Ikarimonogatari, To Destroy the Kingdom of Stormwind
	picture = { reference = "gfx/interface/illustrations/decisions/defias_brotherhood.dds" }
	decision_group_type = major

	desc = legacy_of_defias_brotherhood_decision_desc

	ai_check_interval = 12

	is_shown = {
		title:d_defias_brotherhood.holder = root
		has_faith = faith:masonic
		exists = title:k_stormwind # There are Nobles to go up against
		NOT = { title:k_stormwind.holder = root } # Now that you are the noble yourself, don't wanna fuck yourself up, do ya?
		NOT = { has_character_flag = decided_the_fate_of_the_nobles }
	}

	is_valid = {
		title:d_defias_brotherhood.holder = root
		OR = {
			has_trait = gallowsbait
			has_trait = peasant_leader
		}
		has_faith = faith:masonic
		has_trait = creature_human
		completely_controls = title:c_stormwind
		completely_controls = title:c_northshire
		piety_level >= 3
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
		is_imprisoned = no
		is_at_war = no
	}

	cost = {
		piety = 6000
	}

	effect = {
		custom_tooltip = legacy_of_defias_brotherhood_decision_tt
		trigger_event = {
			id = wc_defias_brotherhood.0001
		}
		add_character_flag = decided_the_fate_of_the_nobles
	}

	ai_potential = {
		is_available_adult = yes
		is_imprisoned = no
		is_at_war = no
	}

	ai_will_do = {
		base = 100
	}
}
