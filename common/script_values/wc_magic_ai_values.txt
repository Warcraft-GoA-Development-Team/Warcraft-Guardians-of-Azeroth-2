
ai_spell_target_character_modifier_value = { 

    value = 0 # Why should I pick this character?

    if = { # More likely to cast on players
        limit = { 
            is_ai = no
        }
        add = 50
    }
    if = { 
        limit = {
            has_character_modifier = important_lore_character
        }
        add = 50
    }

    ### RELATIONS ###
    # save_temporary_opinion_value_as = { name = target_opinion target = scope:ai_caster }
    if = {
        limit = {
            OR = {
                scope:ai_caster = {
                    has_variable = spell_is_harmful
                }
                AND = {
                    scope:ai_caster != this
                    spell_is_harmful_trigger_not_self = {
                        SPELL = scope:ai_caster.var:current_spell_name
                    }
                }
            }
        }

        add = "opinion(scope:ai_caster)"
        multiply = -3

        if = {
            limit = {
                is_vassal_or_below_of = scope:ai_caster 
            }
            add = -25
        }

        if = {
            limit = {
                scope:ai_caster = { has_any_mild_bad_relationship_with_character_trigger = { CHARACTER = prev } }
            }
            add = 25
        }
        else_if = {
            limit = {
                scope:ai_caster = { has_any_moderate_bad_relationship_with_character_trigger = { CHARACTER = prev } }
            }
            add = 50
        }
        else_if = {
            limit = {
                scope:ai_caster = { has_any_worst_bad_relationship_with_character_trigger = { CHARACTER = prev } }
            }
            add = 100
        }
        
        if = {
            limit = {
                OR = {
                    scope:ai_caster = { 
                        any_in_list = {
                            variable = recently_casted_on_by_harmful_targets
                            this = prev
                        }
                    }
                    scope:ai_caster = { is_at_war_with = prev }
                    scope:ai_caster = { is_in_coalition_against_trigger = { CRISIS_HOLDER = prev } }
                }
            }
            add = 250
        }

        # too laggy
        # if = {
        #     limit = {
        #         wc_mp_value < scope:ai_caster.wc_mp_value
        #     }
        #     add = {
        #         value = wc_mp_value
        #         subtract = scope:ai_caster.wc_mp_value
        #         multiply = -2
        #     }
        # }

        if = {
            limit = {
                is_ruler = yes
                military_power > scope:ai_caster.military_power
            }
            add = {
                value = military_power
                subtract = scope:ai_caster.military_power
                multiply = -1
            }
        }

        ### PERSONALITY ###
        if = {
            limit = {
                has_trait = flagellant 
                scope:ai_caster = prev
            }
            add = 100
        }

        ### MAGIC TRAITS ###
        if = {
            limit = {
                is_magic_user_trigger = yes
            }
            add = 50

            if = {
                limit = {
                    scope:ai_caster.var:last_harmful_cast ?= this
                }
                add = 100
            }
            else_if = {
                limit = {
                    scope:ai_caster.var:last_helpful_cast ?= this
                }
                add = -100
            }

            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_light }
                    is_magic_class_user_trigger_no_secret = { MAGIC = shadow }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_shadow }
                    is_magic_class_user_trigger_no_secret = { MAGIC = light }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_death }
                    is_magic_class_user_trigger_no_secret = { MAGIC = life }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_life }
                    is_magic_class_user_trigger_no_secret = { MAGIC = death }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_order }
                    is_magic_class_user_trigger_no_secret = { MAGIC = disorder }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_disorder }
                    is_magic_class_user_trigger_no_secret = { MAGIC = order }
                }
                add = 100
            }
            if = {
                limit = {   
                    scope:ai_caster = { has_variable = magic_user_elemental_fire }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_water }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_elemental_water }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_fire }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_elemental_air }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_earth }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_elemental_earth }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_air }
                }
                add = 100
            }
        }
    }
    else_if = {
        limit = {
            OR = {
                scope:ai_caster = {
                    has_variable = spell_is_helpful
                }
                AND = {
                    scope:ai_caster = this
                    spell_is_helpful_trigger_self = {
                        SPELL = scope:ai_caster.var:current_spell_name
                    }
                }
            }
        }
        add = "opinion(scope:ai_caster)"
        multiply = 3

        if = {
            limit = {
                is_vassal_or_below_of = scope:ai_caster 
            }
            add = 100
        }

        if = {
            limit = {
                scope:ai_caster = { has_any_mild_good_relationship_with_character_trigger = { CHARACTER = prev } }
            }
            add = 25
        }
        else_if = {
            limit = {
                scope:ai_caster = { has_any_moderate_good_relationship_with_character_trigger = { CHARACTER = prev } }
            }
            add = 50
        }
        else_if = {
            limit = {
                scope:ai_caster = { has_any_best_good_relationship_with_character_trigger = { CHARACTER = prev } }
            }
            add = 100
        }

        ### MAGIC TRAITS ###
        if = {
            limit = {
                is_magic_user_trigger = yes
            }
            add = 50

            if = {
                limit = {
                    scope:ai_caster.var:last_harmful_cast ?= this
                }
                add = -100
            }
            else_if = {
                limit = {
                    scope:ai_caster.var:last_helpful_cast ?= this
                }
                add = 100
            }

            if = {
                limit = {
                    scope:ai_caster = { 
                        any_in_list = {
                            variable = recently_casted_on_by_helpful_targets
                            this = prev
                        }
                    }
                }
                add = 250
            }

            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_light }
                    is_magic_class_user_trigger_no_secret = { MAGIC = light }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_shadow }
                    is_magic_class_user_trigger_no_secret = { MAGIC = shadow }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_death }
                    is_magic_class_user_trigger_no_secret = { MAGIC = death }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_life }
                    is_magic_class_user_trigger_no_secret = { MAGIC = life }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_order }
                    is_magic_class_user_trigger_no_secret = { MAGIC = order }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_disorder }
                    is_magic_class_user_trigger_no_secret = { MAGIC = disorder }
                }
                add = 100
            }
            if = {
                limit = {   
                    scope:ai_caster = { has_variable = magic_user_elemental_fire }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_fire }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_elemental_water }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_water }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_elemental_air }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_air }
                }
                add = 100
            }
            if = {
                limit = {
                    scope:ai_caster = { has_variable = magic_user_elemental_earth }
                    is_magic_class_user_trigger_no_secret = { MAGIC = elemental_earth }
                }
                add = 100
            }
        }

    }

    # Ryalois: This is a big change, but major_interset, minor_inrterest and any_neighboring_realm_owner are really expensive so evaluating them from characters that fail has_no_real_status_trigger in my opinion simply is not worth it.
    # Characters that fail has_no_real_status_trigger are basically gameplay irrelevant anyway as spells spent cast on them probably don't affect a playthrough or simulation in any meaningful way.
    if  = {
        limit = {
            has_no_real_status_trigger = yes
        }
        add = -75
    }
    else = {

        if  = {
            limit = {
                scope:ai_caster = { is_of_major_interest_trigger = { CHARACTER = prev } }
            }
            add = 100
        }
        else_if  = {
            limit = {
                scope:ai_caster = { is_of_minor_interest_trigger = { CHARACTER = prev } }
            }
            add = 50
        }
        else_if = {
            limit = {
                any_neighboring_realm_owner = { this = scope:ai_caster  }
            }
            add = 50
        }

        if = {
            limit = {
                is_ruler = yes
            }

            if = {
                limit = {
                    NOR = {
                        is_vassal_or_below_of = scope:ai_caster 
                        is_liege_or_above_of = scope:ai_caster 
                    }
                }
                add = -50
            }
            else_if = {
                limit = { has_variable scope:ai_caster.var:spell_is_ritual }
                add = 50
            }
        }
        else = {
            if = {
                limit = {
                    NOR = {
                        is_courtier_of = scope:ai_caster 
                        is_pool_guest_of = scope:ai_caster 
                    }
                }
                add = -50
            }
            else_if = {
                limit = { has_variable = scope:ai_caster.var:spell_is_ritual }
                add = 50
            }
        }
    }


    # Try and prevent casting on the same person every time
    if = {
        limit = {
            scope:ai_caster.var:last_casted_character ?= this
        }
        add = -1000
    }

    ### MAGIC MODIFIERS ###

    ### EVENT MODIFIERS ###

    ### SPELL SPECIFIC MODIFIERS ###

    if = {
        limit = {
            scope:ai_caster.var:current_spell_class = flag:order
        }
        ## ORDER 

        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:create_portal
                is_travelling = yes
            }
            add = 50
        }
    }
    else_if = {
        limit = {
            scope:ai_caster.var:current_spell_class = flag:disorder
        }
        ## DISORDER

        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:curse
                is_healthy = yes
            }
            
            add = 50
        }
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:subjugate
                this.wc_mp_value > scope:ai_caster.wc_mp_value
            }
            add = 25
        }
        if  = {
            limit = {
                OR = {
                    scope:ai_caster.var:current_spell_name = flag:immolate
                    scope:ai_caster.var:current_spell_name = flag:fire_rain
                }
                scope:ai_caster = { any_war_enemy = { this = prev } }
            }
            add = 50
        }
    }
    else_if = {
        limit = {
            scope:ai_caster.var:current_spell_class = flag:life
        }
        ## LIFE

        # if  = {
        #     limit = {
        #         scope:ai_caster.var:current_spell_name = flag:wildgrowth
        #         any_held_title = {
        #             tier = county
        #             development_level <= bad_development_level
        #         }
        #     }
        #     add = 50
        # }
        if  = {
            limit = {
                OR = {
                    scope:ai_caster.var:current_spell_name = flag:regrowth
                    scope:ai_caster.var:current_spell_name = flag:dreamwalk
                }
                is_landless_adventurer = yes 
            }
            add = 15
        }
        if  = {
            limit = {
                OR = {
                    scope:ai_caster.var:current_spell_name = flag:thrash
                    scope:ai_caster.var:current_spell_name = flag:starfire
                }
                scope:ai_caster = { any_war_enemy = { this = prev } }
            }
            add = 50
        }
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:motw
                scope:ai_caster = { any_knight = { this = prev } }
            }
            add = 50
        }
    }
    else_if = {
        limit = {
            scope:ai_caster.var:current_spell_class = flag:death
        }
        ## DEATH 
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:give_disease
                is_healthy = yes 
            }
            add = 50
        }
    }
    else_if = {
        limit = {
            scope:ai_caster.var:current_spell_class = flag:light
        }
        ## LIGHT
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:salvation
                is_good_salvation_target_trigger = { TARGET = prev }
            }
            add = 250
        }
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:judgement
            }
            add = 25
            this.intrigue > scope:ai_caster.intrigue
        }
        if  = {
            limit = {
                OR = {
                    scope:ai_caster.var:current_spell_name = flag:exorcism
                    scope:ai_caster.var:current_spell_name = flag:avenging_wrath
                }
                OR = {
                    culture = { has_same_culture_heritage = culture:eredruin }
                    culture = { has_same_culture_heritage = culture:scourge }
                    has_trait = being_void
                    has_trait = being_demon
                    has_trait = being_undead
                }
            }
            add = 50
        }
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:avenging_wrath
                scope:ai_caster = { any_war_enemy = { this = prev } }
            }
            add = 50
        }
    }
    else_if = {
        limit = {
            scope:ai_caster.var:current_spell_class = flag:shadow
        }
        ## SHADOW
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:void_leader
                OR = {
                    is_magic_type_criminal_trigger = { MAGICTYPE = shadow } 
                    is_magic_type_shunned_trigger = { MAGICTYPE = shadow } 
                }
            }
            add = -250
        }
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:mutate
                is_healthy = yes
            }
            add = 50
        }
        if  = {
            limit = {
                scope:ai_caster.var:current_spell_name = flag:mind_blast
                scope:ai_caster = { 
                    any_secret = {
                        is_known_by = prev
                        is_blackmailable_secret_trigger = { BLACKMAILER = prev PARTICIPANT = scope:ai_caster }
                    }
                }
            }
            add = 100
        }
    }

    if = {
        limit = {
            OR = {
                scope:ai_caster.var:current_spell_name = flag:summon_fire_elemental
                scope:ai_caster.var:current_spell_name = flag:summon_water_elemental
                #scope:ai_caster.var:current_spell_name = flag:summon_air_elemental
                scope:ai_caster.var:current_spell_name = flag:summon_earth_elemental
            }
        }
        add = -1000
    }

    ## ELEMENTAL FIRE
    ## ELEMENTAL WATER
    ## ELEMENTAL EARTH
    ## ELEMENTAL AIR
}