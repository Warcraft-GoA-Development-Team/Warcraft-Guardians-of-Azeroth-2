## wub wub

faith_is_light = {
	custom_description = {
		text = faith_is_light
		OR = {
			religion = { is_in_family = rf_light }
			#faith = { has_doctrine =  }
		}
	}
}

faith_is_shadow = {
	custom_description = {
		text = faith_is_shadow
		OR = {
			religion = { is_in_family = rf_shadow }
			#faith = { has_doctrine =  }
		}
	}
}

faith_is_life = {
	custom_description = {
		text = faith_is_life
		OR = {
			religion = { is_in_family = rf_life }
			#faith = { has_doctrine =  }
		}
	}
}

faith_is_death = {
	custom_description = {
		text = faith_is_death
		OR = {
			religion = { is_in_family = rf_death }
			#faith = { has_doctrine =  }
		}
	}
}

faith_is_order = {
	custom_description = {
		text = faith_is_order
		OR = {
			religion = { is_in_family = rf_order }
			#faith = { has_doctrine =  }
		}
	}
}

faith_is_disorder = {
	custom_description = {
		text = faith_is_disorder
		OR = {
			religion = { is_in_family = rf_disorder }
			#faith = { has_doctrine =  }
		}
	}
}

faith_is_elemental = {
	custom_description = {
		text = faith_is_elemental
		OR = {
			religion = { is_in_family = rf_elemental }
			#faith = { has_doctrine =  }
		}
	}
}