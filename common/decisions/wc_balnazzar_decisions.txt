# Voluntary entry point: stage the canonical defeat before Sylvanas attacks.
wc_balnazzar_stage_death_decision = {
	ai_check_interval = 12
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = major

	desc = wc_balnazzar_stage_death_decision_desc

	is_shown = {
		this = character:430400 # Balnazzar
		is_alive = yes
		is_landed = yes
		has_title = title:k_lordaeron
		exists = global_var:balnazzar_dreadlord_story
		global_var:balnazzar_dreadlord_story = { story_owner = root }
		NOT = { has_character_flag = wc_balnazzar_staged_death }
		NOT = { has_character_flag = wc_balnazzar_disguise_active }
		NOT = { has_character_flag = balnazzar_became_dathrohan }
		exists = character:430100 # Varimathras
		character:430100 = { is_alive = yes }
		exists = character:42076 # Sylvanas Windrunner
		character:42076 = { is_alive = yes }
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_incapable_trigger = no
		is_at_war = no
	}

	effect = {
		custom_tooltip = wc_balnazzar_stage_death_decision_effect_tooltip
		hidden_effect = {
			trigger_event = wc_balnazzar_story.0002
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 10
		modifier = {
			add = 40
			has_trait = deceitful
		}
		modifier = {
			add = 20
			has_trait = patient
		}
	}
}

# Adventurer decision after losing Lordaeron; its controller chooses when to
# take Dathrohan's place.
balnazzar_become_dathrohan_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	# is_landless_ruler = yes below means only an adventurer ever sees this - group it
	# with the other adventurer-only major decisions instead of the generic major list.
	decision_group_type = adventurer

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 12
		kingdom = 12
		empire = 12
		hegemony = 12
	}

	desc = balnazzar_become_dathrohan_decision_desc

	is_shown = {
		exists = global_var:balnazzar_dreadlord_story
		global_var:balnazzar_dreadlord_story = { story_owner = root }
		NOT = { has_character_flag = wc_balnazzar_disguise_active }
		NOT = { has_character_flag = balnazzar_became_dathrohan }
		is_landless_ruler = yes
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		balnazzar_become_dathrohan_effect = yes
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 10
		modifier = {
			add = 40
			has_trait = deceitful
		}
		modifier = {
			add = 20
			has_trait = ambitious
		}
	}
}

balnazzar_move_against_alexandros_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	# Major decision - starts assassination scheme, point-of-no-return
	decision_group_type = major

	# Continue checking after promotion, including a county-tier Dathrohan.
	ai_check_interval_by_tier = {
		barony = 0
		county = 24
		duchy = 24
		kingdom = 24
		empire = 24
		hegemony = 24
	}

	desc = balnazzar_move_against_alexandros_decision_desc

	is_shown = {
		has_character_flag = wc_balnazzar_disguise_active
		this = character:60021
		exists = global_var:balnazzar_dreadlord_story
		global_var:balnazzar_dreadlord_story = { story_owner = root }
		has_character_flag = balnazzar_kelthuzad_pact_sealed
		exists = character:60017 # Alexandros Mograine
		character:60017 = { is_alive = yes }
		# Hide only once Balnazzar's murder scheme against Alexandros is already running.
		# An unrelated murder scheme must not block this story decision.
		NOT = {
			any_scheme = {
				type = murder
				scheme_target_character = character:60017
				count > 0
			}
		}
	}

	# CK3 bug: bare is_alive=yes doesn't render in Requirements panel - wrap in custom_tooltip
	is_valid = {
		custom_tooltip = {
			text = balnazzar_move_against_alexandros_decision_requirement_alive
			exists = character:60017
			character:60017 = { is_alive = yes }
		}
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		custom_tooltip = balnazzar_move_against_alexandros_decision_effect_tooltip
		if = {
			limit = { character:60019 = { is_alive = yes } }
			trigger_event = { id = wc_balnazzar_story.1005 }
		}
		else = {
			start_scheme = { type = murder target_character = character:60017 }
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 50
		modifier = {
			add = 25
			OR = {
				has_trait = wrathful
				has_trait = vengeful
			}
		}
		modifier = {
			add = 15
			OR = {
				has_trait = deceitful
				has_trait = ambitious
			}
		}
	}
}

# Reconvene after the automatic first council; the story owns the delay.
wc_balnazzar_stratholme_council_decision = {
	ai_check_interval = 6
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions
	desc = wc_balnazzar_stratholme_council_decision_desc

	is_shown = {
		wc_balnazzar_can_hold_stratholme_council_trigger = yes
		global_var:balnazzar_dreadlord_story = {
			exists = var:wc_balnazzar_stratholme_council_seen
		}
	}
	is_valid = {
		global_var:balnazzar_dreadlord_story = {
			NOT = {
				AND = {
					exists = var:wc_balnazzar_stratholme_council_open
					var:wc_balnazzar_stratholme_council_open = root
				}
			}
		}
	}
	effect = {
		trigger_event = { id = wc_balnazzar_story.1015 }
	}
	ai_potential = { always = yes }
	ai_will_do = {
		base = 75
	}
}

# Voluntary upkeep for an active disguise under suspicion.
wc_balnazzar_maintain_mask_decision = {
	ai_check_interval = 6
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	desc = wc_balnazzar_maintain_mask_decision_desc

	cooldown = { years = 1 }

	is_shown = {
		has_character_flag = wc_balnazzar_disguise_active
		exists = global_var:balnazzar_dreadlord_story
		global_var:balnazzar_dreadlord_story = {
			story_owner = root
			exists = var:wc_balnazzar_suspicion
			var:wc_balnazzar_suspicion > 0
		}
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		add_stress = medium_stress_gain
		hidden_effect = {
			if = {
				limit = { wc_balnazzar_free_witness_count > 0 }
				wc_balnazzar_change_suspicion_effect = { CHANGE = -2 }
			}
			else = { wc_balnazzar_change_suspicion_effect = { CHANGE = -10 } }
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 25
		modifier = {
			add = 25
			global_var:balnazzar_dreadlord_story = { var:wc_balnazzar_suspicion >= 30 }
		}
		modifier = {
			add = 25
			global_var:balnazzar_dreadlord_story = { var:wc_balnazzar_suspicion >= 60 }
		}
		modifier = {
			add = 15
			has_trait = paranoid
		}
		modifier = {
			factor = 0.5
			stress_level >= 2
		}
	}
}

# A route prepared before the crisis can still be used afterward.
wc_balnazzar_use_prepared_body_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions
	desc = wc_balnazzar_use_prepared_body_decision_desc
	ai_check_interval = 1
	is_shown = {
		has_character_flag = wc_balnazzar_disguise_active
		exists = var:wc_nathrezim_prepared_body
		exists = global_var:balnazzar_dreadlord_story
		global_var:balnazzar_dreadlord_story = { story_owner = root }
	}
	is_valid = {
		wc_nathrezim_can_escape_cover_trigger = yes
		global_var:balnazzar_dreadlord_story = { NOT = { exists = var:wc_balnazzar_exposure_queued } }
	}
	effect = { wc_balnazzar_escape_cover_effect = yes }
	ai_potential = { always = yes }
	ai_will_do = {
		base = 0
		modifier = { add = 80 has_variable = wc_nathrezim_cover_crisis }
		modifier = { add = 20 age >= 70 }
	}
}

# Voluntary reveal - gated on high stress or active inquiry.
balnazzar_shed_the_mask_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 12
		kingdom = 12
		empire = 12
		hegemony = 12
	}

	desc = balnazzar_shed_the_mask_decision_desc

	is_shown = {
		has_character_flag = wc_balnazzar_disguise_active
		OR = {
			stress_level >= 2
			has_character_modifier = wc_balnazzar_suspicion_inquiry_modifier
		}
		NOT = { has_character_flag = balnazzar_shed_the_mask }
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_incapable_trigger = no
	}

	# CK3 quirk: bare trigger_event blanks Effects tooltip - wrap in custom_tooltip
	effect = {
		custom_tooltip = balnazzar_shed_the_mask_decision_effect_tooltip

		trigger_event = {
			id = wc_balnazzar_story.2000
			days = 3
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 10

		modifier = {
			add = 40
			has_trait = wrathful
		}
		modifier = {
			add = 40
			has_trait = paranoid
		}
	}
}
