is_obedient = {
    has_relation_flag = {
        target = $TARGET$
        relation = $RELATION$
        flag = obedient
    }
}

# Add relationships that can force obedience here
is_obedient_any = {
    OR = {
        is_obedient = { TARGET = $TARGET$ RELATION = master }
    }
}