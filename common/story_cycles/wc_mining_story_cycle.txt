mining_story_cycle = {
	on_setup = {
		story_owner = {
			save_profession_effect = {
				PROFESSION = flag:Mining
			}
			setup_profession_vars_effect = {
				PROFESSION = flag:mining
			}
			trigger_event = wc_profession.001
		}
		if = {
			limit = { NOT = { has_global_variable = mining_reagents_done } }
			# so we dont load global variables w reagent lists no one is using
			init_mining_reagents_effect = yes
		}
	}

	on_end = {
		#remove profession vars
	}

	on_owner_death = {}

	# effect_group = {
	# 	days = 60 
	# }
}