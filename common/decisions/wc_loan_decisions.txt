get_loan_decision = {
	picture = "gfx/interface/illustrations/decisions/goblo.dds"
        desc = get_loan_decision_desc
        cooldown = { days = 1095 }

	is_shown = {
		
	}
	is_valid_showing_failures_only = {
		is_imprisoned = no
	}
        
      sort_order = 0

	effect = {
		custom_tooltip = get_loan_decision
		remove_short_term_gold = 5
		add_gold = 300
	}

	ai_check_interval = 0
}
