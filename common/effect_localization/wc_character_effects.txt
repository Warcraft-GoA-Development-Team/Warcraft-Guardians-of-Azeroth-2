set_relation_master = {
    global = GET_MASTER
    global_past = GOT_MASTER
    first = I_GET_MASTER
    first_past = I_GOT_MASTER
    third = THEY_GET_MASTER
    third_past = THEY_GOT_MASTER
}

remove_relation_master = {
    global = LOSE_MASTER
    first = I_LOSE_MASTER
    third = THEY_LOSE_MASTER
    global_past = LOST_MASTER
}

set_relation_summoner = {
    global = GET_SUMMONER
    global_past = GOT_SUMMONER
    first = I_GET_SUMMONER
    first_past = I_GOT_SUMMONER
    third = THEY_GET_SUMMONER
    third_past = THEY_GOT_SUMMONER
}

remove_relation_summoner = {
    global = LOSE_SUMMONER
    first = I_LOSE_SUMMONER
    third = THEY_LOSE_SUMMONER
    global_past = LOST_SUMMONER
}