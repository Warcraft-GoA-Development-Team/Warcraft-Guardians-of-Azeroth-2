gh_dynamic_terrain_mask_0 = {
    usage = game

    priority = 255

    dynamic_terrain_mask_element_0 = {
        dna_modifiers = {
            morph = {
                mode = add
                gene = gene_gh_dynamic_terrain_mask_0
                template = dynamic_terrain_marker_0
                # Passing gh_sun_brightness inverted to shaders, so that the sun is fully bright in map editor
                # and other such contexts where the corresponding decal's weight will be 0 by default.
                value = {
                    value = 0.0

                    if = {
                        limit = {
                            OR = {
                                NOT = { has_global_variable = mists_of_pandaria_parted_variable }
                                global_var:mists_of_pandaria_parted_variable = no
                            }
                        }

                        add = 1
                    }

                    min = gh_dynamic_terrain_mask_unit_value
                }
            }
        }

        weight = {
            base = 1
        }
    }
}