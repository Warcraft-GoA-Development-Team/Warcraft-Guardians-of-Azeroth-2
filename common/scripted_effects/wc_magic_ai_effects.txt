decide_to_cast_spell_effect = { # Stage 2
    random_list = {
        100 = { # Yes
            ai_will_cast_modifier = yes
            if = {
                limit = {
                    debug_only = yes 
                }
                change_global_variable = {
                    name = ai_spells_pre_cast_stage_2
                    add = 1
                }
            }
            remove_magic_variables = yes
            select_spell_class_ai_effect = yes
        }
        100 = { # Nahh
            ai_wont_cast_modifier = yes
            trigger_event = {
                on_action = wc_on_ai_spellcasting
                days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
            } 
        }
    }
}

set_last_elemental_type_effect = {
    if = {
        limit = {
            exists = var:current_spell_class
            OR = {
                var:current_spell_class = flag:elemental_air
                var:current_spell_class = flag:elemental_water
                var:current_spell_class = flag:elemental_fire
                var:current_spell_class = flag:elemental_earth
            }
            exists = var:last_elemental_type
            is_ai = no
        }
        set_variable = {
            name = elemental_type
            value = var:last_elemental_type
        }
    }
    if = {
        limit = {
            is_ai = yes
        }
        random_list = {
            10 = {
                ai_spell_spirit_modifier = yes
                set_variable = {
                    name = elemental_type
                    value = flag:spirit
                }
                set_variable = {
                    name = last_elemental_type
                    value = flag:spirit
                }
            }
            10 = {
                ai_spell_decay_modifier = yes
                set_variable = {
                    name = elemental_type
                    value = flag:decay
                }
                set_variable = {
                    name = last_elemental_type
                    value = flag:decay
                }
            }
        }
    }
}

select_spell_class_ai_effect = { # Stage 3
    save_scope_as = ai_caster
    if = { # Decide to change the caster now!
        limit = {
            OR = {
                has_summoned_demon_trigger = yes
                has_subjugated_trigger = yes
            }
        }
        random_list = {
            10 = { # Keep as root   
                save_scope_as = real_caster
            }
            10 = {
                trigger = {
                    any_relation = {
                        type = summon
                        is_any_spell_castable_trigger = yes
                    }
                    NOR = { 
                        wc_convoke_active_trigger = yes
                        spell_hack_activated_trigger = yes
                    }
                }
                random_relation = {
                    type = summon
                    save_scope_as = real_caster
                }
            }
            10 = {
                trigger = {
                    any_relation = {
                        type = thrall
                        is_any_spell_castable_trigger = yes
                    }
                    NOR = { 
                        wc_convoke_active_trigger = yes
                        spell_hack_activated_trigger = yes
                    }
                }
                random_relation = {
                    type = thrall
                    save_scope_as = real_caster
                }
            }
        }
    }
    random_list = {
        # Life
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger = { MAGIC = life }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = life }
            set_variable = {
                name = current_spell_class
                value = flag:life
            }
        }
        # Death
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger = { MAGIC = death }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = death }
            set_variable = {
                name = current_spell_class
                value = flag:death
            }
        }
        # Disorder
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger = { MAGIC = disorder }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = disorder }
            set_variable = {
                name = current_spell_class
                value = flag:disorder
            }
        }
        # Order
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger = { MAGIC = order }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = order }
            set_variable = {
                name = current_spell_class
                value = flag:order
            }
        }
        # Light
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger = { MAGIC = light }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = light }
            set_variable = {
                name = current_spell_class
                value = flag:light
            }
        }
        # Shadow
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger = { MAGIC = shadow }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = shadow }
            set_variable = {
                name = current_spell_class
                value = flag:shadow
            }
        }
        # Elemental F
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger_elemental = { MAGIC = elemental_fire }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = elemental_fire }
            set_variable = {
                name = current_spell_class
                value = flag:elemental_fire
            }
        }
        # Elemental A
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger_elemental = { MAGIC = elemental_air }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = elemental_air }
            set_variable = {
                name = current_spell_class
                value = flag:elemental_air
            }
        }
        # Elemental E 
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger_elemental = { MAGIC = elemental_earth }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = elemental_earth }
            set_variable = {
                name = current_spell_class
                value = flag:elemental_earth
            }
        }
        # Elemental W
        10 = {
            trigger = {
                OR = {
                    is_magic_class_user_trigger_elemental = { MAGIC = elemental_water }
                    wc_convoke_active_trigger = yes
                    spell_hack_activated_trigger = yes
                }
            }
            ai_spell_class_modifier = { CLASS = elemental_water }
            set_variable = {
                name = current_spell_class
                value = flag:elemental_water
            }
        }
    }

    if = {
        limit = {
            has_variable = current_spell_class
        }
        set_last_elemental_type_effect = yes
        if = {
            limit = {
                debug_only = yes
            }
            change_global_variable = {
                name = ai_spells_pre_cast_stage_3
                add = 1
            }
        }
        select_spell_ai_effect = yes
    }
    else = {
        # Come back later
        trigger_event = {
            on_action = wc_on_ai_spellcasting
            days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
        }
    }
}

select_spell_ai_effect = { # Stage 4 
    save_scope_as = real_caster
    switch = {
        trigger = var:current_spell_class
        flag:order = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = Order_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = Order_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:disorder = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = disorder_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = disorder_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:life = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = life_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = life_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:death = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = death_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = death_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:light = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = light_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = light_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:shadow = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = shadow_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = shadow_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:elemental_air = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = elemental_air_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = elemental_air_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:elemental_earth = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = elemental_earth_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = elemental_earth_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:elemental_fire = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = elemental_fire_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = elemental_fire_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
        flag:elemental_water = {
            if = {
                limit = {
                    any_in_global_list = {
                        variable = elemental_water_spells
                        save_temporary_scope_as = my_spell
                        root = { can_select_spell_trigger = { SPELL = scope:my_spell } }
                    }
                }
                random_in_global_list = {
                    variable = elemental_water_spells
                    save_scope_as = cur_spell
                    weight = {
                        base =  1
                        ai_spell_modifier = { CASTER = root SPELL = this }
                    }
                    root = { 
                        set_variable = {
                            name = current_spell_name
                            value = scope:cur_spell
                        }
                    }
                }
            }
        }
    }

    if = {
        limit = {
            has_variable = current_spell_name
        }
        if = {
            limit = {
                debug_only = yes
            }
            change_global_variable = {
                name = ai_spells_pre_cast_stage_4
                add = 1
            }
        }
        select_spell_rank_ai_effect = yes
    }
    else = {
        # Come back later
        trigger_event = {
            on_action = wc_on_ai_spellcasting
            days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
        }
    }
}

set_basic_spell_rank_effect = {
    if = {
        limit = {
            NOT = {
                spell_has_ranks_trigger = {
                    SPELL = var:current_spell_name
                }
            }
        }
        set_variable = {
            name = current_spell_rank
            value = 0
        }
    }
    else = {
        set_variable = {
            name = current_spell_rank
            value = 1
        }
    }
}

set_spell_target_type_effect = {
    if = {
        limit = {
            has_variable = current_target_type
        }
        set_variable = {
            name = old_target_type
            value = var:current_target_type
        }
    }
    else = {
        set_variable = {
            name = old_target_type
            value = flag:poop
        }
    }
    # getting the target type by rank
    if = {
        limit = { spell_target_is_character_list_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank } }
        set_variable = { name = current_target_type value = flag:character_list }
    }
    else_if = {
        limit = { spell_target_is_province_or_title_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank } }
        set_variable = { name = current_target_type value = flag:title }
    }
    else_if = {
        limit = { spell_target_is_character_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank } }
        set_variable = { name = current_target_type value = flag:character }
    }
    else_if = {
        limit = { spell_target_is_province_or_title_list_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank } }
        set_variable = { name = current_target_type value = flag:title_list }
    }
    else_if = {
        limit = {
            spell_target_is_enemy_army_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank }
            spell_target_is_friendly_army_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank }
        }
        set_variable = { name = current_target_type value = flag:army }
    }
    else_if = {
        limit = {
            spell_target_is_friendly_army_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank }
        }
        set_variable = { name = current_target_type value = flag:friendly_army }
    }
    else_if = {
        limit = {
            spell_target_is_enemy_army_trigger = { SPELL = var:current_spell_name RANK_VALUE = var:current_spell_rank }
        }
        set_variable = { name = current_target_type value = flag:enemy_army }
    }
    execute_scoped_spell_effect = {
        SPELL = var:current_spell_name
        EFFECT = set_spell_mana_cost_variable_effect
    }
    if = {
        limit = {
            OR = {
                var:old_target_type = flag:character
                var:old_target_type = flag:poop
            }
            NOT = { var:current_target_type = flag:character }
            NOT = { exists = var:original_recipient }
            exists = var:spell_recipient    
        }
        set_variable = {
            name = original_recipient
            value = var:spell_recipient
        }
        remove_variable = spell_recipient
    }
    else_if = {
        limit = {
            var:current_target_type = flag:character
            NOT = { var:old_target_type = flag:character }
            exists = var:original_recipient
        }
        set_variable = {
            name = spell_recipient
            value = var:original_recipient
        }
        remove_variable = original_recipient
    }
    else_if = {
        limit = {
            OR = {
                var:current_target_type = flag:title_list
                var:current_target_type = flag:title
                var:current_target_type = flag:army
                var:current_target_type = flag:enemy_army
                var:current_target_type = flag:friendly_army
            }
            OR = {
                var:old_target_type = flag:title_list
                var:old_target_type = flag:title
                var:old_target_type = flag:army
                var:old_target_type = flag:enemy_army
                var:old_target_type = flag:friendly_army
            }
            NOT = { var:old_target_type = var:current_target_type }
            exists = var:original_recipient
            exists = var:spell_recipient
        }
        remove_variable = spell_recipient
    }
    # something for lists ?
    remove_variable = old_target_type
}

select_spell_rank_ai_effect = { # Stage 5
    set_basic_spell_rank_effect = yes
    if = {
        limit = {
            spell_has_ranks_trigger = { SPELL = var:current_spell_name }
            OR = {
                any_in_list = { list = known_rank_2_spells this = var:current_spell_name }
                any_in_list = { list = known_rank_3_spells this = var:current_spell_name }
            }
        }
        random_list = {
            30 = {
                trigger = {
                    any_in_list = { list = known_rank_2_spells this = var:current_spell_name }
                }
                set_variable = {
                    name = current_spell_rank
                    value = 2
                }
            }
            30 = {
                trigger = {
                    any_in_list = { list = known_rank_3_spells this = var:current_spell_name }
                }
                set_variable = {
                    name = current_spell_rank
                    value = 3
                }
            }
            30 = { 
                # stay at rank 1
            }
        }
    }
    set_spell_target_type_effect = yes
    if = {
        limit = {
            has_variable = current_spell_rank
            has_variable = current_target_type
        }
        if = {
            limit = {
                debug_only = yes
            }
            change_global_variable = {
                name = ai_spells_pre_cast_stage_5
                add = 1
            }
        }
        select_target_ai_effect = yes
    }
    else = {
        # Come back later
        trigger_event = {
            on_action = wc_on_ai_spellcasting
            days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
        }
    }
}

get_targets_amount_effect = {
    set_variable = {
        name = targets_amount
        value = 1
    }
    random_list = {
        30 = { 
            trigger = { }
        }
        30 = {
            change_variable = {
                name = targets_amount
                add = 1
            }
        }   
        30 = {
            change_variable = {
                name = targets_amount
                add = 2
            }
        }
    }
}

get_titles_list_effect = {
    save_scope_as = caster
    if = {
        limit = {
            has_variable_list = target_titles
        }
        clear_variable_list = target_titles
    }
    if = {
        limit = {
            exists = var:original_recipient
            NOT = { var:current_spell_name ?= flag:create_portal }
        }
        var:original_recipient = {
            save_scope_as = spell_target
        }
        scope:spell_target = {
            if = {
                limit = {
                    scope:caster = {
                        spell_target_title_subtype_province_trigger = {
                            SPELL = var:current_spell_name
                            RANK_VALUE = var:current_spell_rank
                        }
                    }
                }
                every_held_title = {
                    limit = {
                        tier = tier_barony
                        satisfy_spell_triggers_barony = { SPELL = scope:caster.var:current_spell_name }
                    }
                    save_scope_as = potential_title
                    scope:caster = {
                        add_to_variable_list = {
                            name = target_titles
                            target = scope:potential_title
                        }
                    }
                }
                if = {
                    limit = {
                        scope:caster = {
                            NOT = { has_variable = direct_only }
                        }
                    }
                    every_vassal_or_below = {
                        every_held_title = {
                            limit = {
                                tier = tier_barony
                                satisfy_spell_triggers_barony = { SPELL = scope:caster.var:current_spell_name }
                            }
                            save_scope_as = potential_title
                            scope:caster = {
                                add_to_variable_list = {
                                    name = target_titles
                                    target = scope:potential_title
                                }
                            }
                        }
                    }
                }
            }
            else_if = {
                limit = {
                    scope:caster = {
                        spell_target_title_subtype_county_trigger = {
                            SPELL = var:current_spell_name
                            RANK_VALUE = var:current_spell_rank
                        }
                    }
                }
                every_held_title = {
                    limit = {
                        tier = tier_county
                    }
                    save_scope_as = potential_title
                    scope:caster = {
                        add_to_variable_list = {
                            name = target_titles
                            target = scope:potential_title
                        }
                    }
                }
                if = {
                    limit = {
                        scope:caster = {
                            NOT = { has_variable = direct_only }
                        }
                    }
                    every_vassal_or_below = {
                        every_held_title = {
                            limit = {
                                tier = tier_county
                            }
                            save_scope_as = potential_title
                            scope:caster = {
                                add_to_variable_list = {
                                    name = target_titles
                                    target = scope:potential_title
                                }
                            }
                        }
                    }
                }
            }
            else_if = {
                limit = {
                    scope:caster = {
                        spell_target_title_subtype_duchy_trigger = {
                            SPELL = var:current_spell_name
                            RANK_VALUE = var:current_spell_rank
                        }
                    }
                }
                every_held_title = {
                    limit = {
                        tier = tier_duchy
                    }
                    save_scope_as = potential_title
                    scope:caster = {
                        add_to_variable_list = {
                            name = target_titles
                            target = scope:potential_title
                        }
                    }
                }
                if = {
                    limit = {
                        scope:caster = {
                            NOT = { has_variable = direct_only }
                        }
                    }
                    every_vassal_or_below = {
                        every_held_title = {
                            limit = {
                                tier = tier_duchy
                            }
                            save_scope_as = potential_title
                            scope:caster = {
                                add_to_variable_list = {
                                    name = target_titles
                                    target = scope:potential_title
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    else_if = {
        limit = {
            var:current_spell_name = flag:create_portal
        }
        if = {
            limit = {
                has_variable = previous_target
            }
            remove_variable = previous_target
        }
        every_empire = {
            limit = {
                any_in_de_jure_hierarchy = {
                    tier = tier_barony
                }
                exists = holder
                holder = {
                    is_landed = yes
                    in_diplomatic_range_trigger = { target = scope:caster }
                    is_independent_ruler = yes
                    highest_held_title_tier = tier_empire
                }
            }
            save_scope_as = potential_title
            scope:caster = {
                add_to_variable_list = {
                    name = target_titles
                    target = scope:potential_title
                }
            }
        }
        every_kingdom = {
            limit = {
                any_in_de_jure_hierarchy = {
                    tier = tier_barony
                }
                exists = holder
                holder = {
                    is_landed = yes
                    in_diplomatic_range_trigger = { target = scope:caster }
                    is_independent_ruler = yes
                    highest_held_title_tier = tier_kingdom
                }
            }
            save_scope_as = potential_title
            scope:caster = {
                add_to_variable_list = {
                    name = target_titles
                    target = scope:potential_title
                }
            }
        }
        every_duchy = {
            limit = {
                any_in_de_jure_hierarchy = {
                    tier = tier_barony
                }
                exists = holder
                holder = {
                    is_landed = yes
                    in_diplomatic_range_trigger = { target = scope:caster }
                    is_independent_ruler = yes
                    highest_held_title_tier = tier_duchy
                }
            }
            save_scope_as = potential_title
            scope:caster = {
                add_to_variable_list = {
                    name = target_titles
                    target = scope:potential_title
                }
            }
        }
        every_county = {
            limit = {
                any_in_de_jure_hierarchy = {
                    tier = tier_barony
                }
                exists = holder
                holder = {
                    is_landed = yes
                    in_diplomatic_range_trigger = { target = scope:caster }
                    is_independent_ruler = yes
                    highest_held_title_tier = tier_county
                }
            }
            save_scope_as = potential_title
            scope:caster = {
                add_to_variable_list = {
                    name = target_titles
                    target = scope:potential_title
                }
            }
        }
    }
}

get_armies_list_effect = {
    save_scope_as = caster
    if = {
        limit = {
            NOT = { exists = var:original_recipient }
        }
        set_variable = {
            name = original_recipient
            value = var:spell_recipient
        }
        remove_variable = spell_recipient
    }
    if = {
        limit = {
            has_variable_list = target_armies
        }
        clear_variable_list = target_armies
    }

    var:original_recipient = {
        save_scope_as = army_target
    }
    if = {
        limit = {
            var:current_target_type = flag:friendly_army
        }
        scope:army_target = {
            every_war_ally = {
                every_army = {
                    save_scope_as = this_army
                    var:real_caster ?= {
                        add_to_variable_list = {
                            name = target_armies
                            target = scope:this_army
                        }
                    }
                }
            }
        }
    }
    else_if = {
        limit = {
            var:current_target_type = flag:enemy_army
        }
        scope:army_target = {
            every_army = {
                save_scope_as = this_army
                scope:caster ?= {
                    add_to_variable_list = {
                        name = target_armies
                        target = scope:this_army
                    }
                }
            }
            every_war_enemy = {
                every_army = {
                    save_scope_as = this_army
                    scope:caster ?= {
                        add_to_variable_list = {
                            name = target_armies
                            target = scope:this_army
                        }
                    }
                }
            }
        }
    }
    else_if = {
        limit = {
            var:current_target_type = flag:army
        }
        scope:army_target = {
            every_war_enemy = {
                every_army = {
                    save_scope_as = this_army
                    scope:caster ?= {
                        add_to_variable_list = {
                            name = target_armies
                            target = scope:this_army
                        }
                    }
                }
            }
            every_war_ally = {
                every_army = {
                    save_scope_as = this_army
                    scope:caster ?= {
                        add_to_variable_list = {
                            name = target_armies
                            target = scope:this_army
                        }
                    }
                }
            }
            every_army = {
                save_scope_as = this_army
                scope:caster ?= {
                    add_to_variable_list = {
                        name = target_armies
                        target = scope:this_army
                    }
                }
            }
        }

    }
}

select_target_ai_effect = { # Stage 6
    set_variable = eligible_only 
    set_variable = {
        name = real_caster 
        value = this
    }
    get_recommended_targets_list = yes
    get_targets_amount_effect = yes
    if = {
        limit = { 
            spell_hack_activated_trigger = no
        }
        if = {
            limit = {
                OR = {
                    var:current_target_type = flag:character
                    var:current_target_type = flag:title
                    var:current_target_type = flag:title_list
                    var:current_target_type = flag:friendly_army
                    var:current_target_type = flag:army
                    var:current_target_type = flag:enemy_army
                }
            }
            random_in_list = {
                weight = {
                    base =  1
                    ai_spell_target_character_modifier = { TARGET = scope:spell_target CASTER = root SPELL = var:current_spell_name }
                }
                variable = recommended_targets_list
                save_scope_as = spell_target
                scope:ai_caster = {
                    set_variable = {
                        name = spell_recipient
                        value = scope:spell_target
                    }
                    add_to_recently_casted_list_effect = { TARGET = scope:spell_target }
                }
            }
            # if = {
            #     limit = {
            #         exists = var:spell_recipient
            #         ai_spell_target_character_modifier_value <= -50
            #     }
            #     set_variable = cancel_spell_ai
            # }
        }
        else_if = {
            limit = {
                var:current_target_type = flag:character_list
            }
            while = {
                count = var:targets_amount
                random_in_list = {
                    limit = {
                        any_in_list = { 
                            list = spell_targets_list 
                            save_temporary_scope_as = my_target
                            NOT = { this = scope:my_target }
                        }
                    }
                    weight = {
                        base =  1
                        ai_spell_target_character_modifier = { TARGET = scope:spell_target CASTER = root SPELL = var:current_spell_name }
                    }
                    variable = recommended_targets_list
                    save_scope_as = spell_target
                    scope:ai_caster = {
                        add_to_variable_list = {
                            name = spell_targets_list
                            target = scope:spell_target
                        }
                    }
                }
            }
        }

        if = {
            limit = {
                var:current_target_type = flag:title
                exists = var:spell_recipient
            }
            set_variable = {
                name = original_recipient
                value = var:spell_recipient
            }
            remove_variable = spell_recipient
            get_titles_list_effect = yes
            random_in_list = {
                # TODO_MAGIC_AI: Add title weight modifier
                # weight = {
                #     base =  1
                #     ai_spell_target_character_modifier = { TARGET = scope:spell_target CASTER = root SPELL = var:current_spell_name }
                # }
                variable = target_titles
                save_scope_as = spell_target
                scope:ai_caster = {
                    set_variable = {
                        name = spell_recipient
                        value = scope:spell_target
                    }
                }
            }
        }
        else_if = {
            limit = {
                var:current_target_type = flag:title_list
                exists = var:spell_recipient
            }
            set_variable = {
                name = original_recipient
                value = var:spell_recipient
            }
            remove_variable = spell_recipient
            get_titles_list_effect = yes
            while = {
                count = var:targets_amount
                random_in_list = {
                    limit = {
                        any_in_list = { 
                            list = spell_targets_list 
                            NOT = { this = scope:spell_target }
                        }
                    }
                    # TODO_MAGIC_AI: Add title weight modifier
                    # weight = {
                    #     base =  1
                    #     ai_spell_target_character_modifier = { TARGET = scope:spell_target CASTER = root SPELL = var:current_spell_name }
                    # }
                    variable = target_titles
                    save_scope_as = spell_target
                    scope:ai_caster = {
                        add_to_variable_list = {
                            name = spell_targets_list
                            target = scope:spell_target
                        }
                    }
                }
            }
        }
        else_if = {
            limit = {
                OR = {
                    var:current_target_type = flag:friendly_army
                    var:current_target_type = flag:army
                    var:current_target_type = flag:enemy_army
                }
                exists = var:spell_recipient
            }
            set_variable = {
                name = original_recipient
                value = var:spell_recipient
            }
            remove_variable = spell_recipient
            get_armies_list_effect = yes
            random_in_list = {
                # TODO_MAGIC_AI: Add title weight modifier
                # weight = {
                #     base =  1
                #     ai_spell_target_character_modifier = { TARGET = scope:spell_target CASTER = root SPELL = var:current_spell_name }
                # }
                variable = target_armies
                save_scope_as = spell_target
                scope:ai_caster = {
                    set_variable = {
                        name = spell_recipient
                        value = scope:spell_target
                    }
                }
            }
        }
        
    }
    if = {
        limit = {
            OR = {
                spell_hack_activated_trigger = yes
                AND = {
                    exists = var:spell_recipient
                    OR = {
                        var:current_target_type = flag:character
                        var:current_target_type = flag:title
                        var:current_target_type = flag:friendly_army
                        var:current_target_type = flag:army
                        var:current_target_type = flag:enemy_army
                    }
                }
                AND = {
                    has_variable_list = spell_targets_list
                    NOT = { exists = var:spell_recipient }
                    OR = {
                        var:current_target_type = flag:title_list
                        var:current_target_type = flag:character_list
                    }
                }
            }
        }
        if = {
            limit = {
                debug_only = yes
            }
            change_global_variable = {
                name = ai_spells_pre_cast_stage_6
                add = 1
            }
        }
        if = {
            limit = {
                NOT = { exists = var:cancel_spell_ai }
            }
            set_secondary_variables_ai_effect = yes
        }
        else = {
            remove_variable = cancel_spell_ai
            # Come back later
            trigger_event = {
                on_action = wc_on_ai_spellcasting
                days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
            }
        }
    }
    else = {
        # Come back later
        trigger_event = {
            on_action = wc_on_ai_spellcasting
            days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
        }
    }
    # TODO_MAGIC_AI: Select non-character & list options
}


# TODO_MAGIC_AI: Move set_target_or_add_sgui stuff here
set_multiple_targets_ai_effect = {

}

set_secondary_variables_ai_effect = { # Stage 7
    set_spell_secondary_interaction_effect = { SPELL = var:current_spell_name }
        if = {
            limit = {
                debug_only = yes
            }
            change_global_variable = {
                name = ai_spells_pre_cast_stage_7
                add = 1
            }
        }
        cast_spell_effect_ai = yes
}

cast_spell_sgui_effect = {
    if = {
        limit = {
            NOR = {
                spell_hack_activated_trigger = yes
                wc_presence_of_mind_activated_trigger = yes
                wc_reverse_causality_activated_trigger = yes
            }
        }
        execute_scoped_spell_effect = {
            EFFECT = spell_cast_effect
            SPELL = var:current_spell_name
        }
    }
    else_if = {
        limit = {
            spell_hack_activated_trigger = yes
        }
        execute_scoped_spell_effect = {
            EFFECT = wc_learn_spell_effect_external
            SPELL = var:current_spell_name
        }
        remove_variable = wc_spell_hack_activated
    }
    else_if = {
        limit = {
            wc_presence_of_mind_activated_trigger = yes
        }
        execute_scoped_spell_effect = {
            EFFECT = wc_halve_cooldown_effect
            SPELL = var:current_spell_name
        }
        remove_variable = wc_presence_of_mind_activated
        trigger_event = {
            on_action = regenerate_presence_of_mind
            years = 1
        }
    }
    else = {
        execute_scoped_spell_effect = {
            EFFECT = wc_reverse_causality_effect
            SPELL = var:current_spell_name
        }
        remove_variable = wc_reverse_causality_activated
        trigger_event = {
            on_action = regenerate_reverse_causality
            years = 5
        }
    }
    if = {
        limit = {
            is_ai = yes
            has_variable = current_secondary_interaction_target
        }
        switch = {
            trigger = var:current_secondary_interaction

            flag:wc_magic_fire_shield_interaction = {
                run_interaction = {
                    interaction = wc_magic_fire_shield_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                    execute_threshold = accept
                }
            }

            flag:wc_magic_give_disease_interaction = {
                run_interaction = {
                    interaction = wc_magic_give_disease_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                    execute_threshold = accept
                }
            }

            flag:wc_magic_epidemic_interaction = {
                run_interaction = {
                    interaction = wc_magic_epidemic_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                    execute_threshold = accept
                }
            }

            flag:wc_magic_druid_forms_interaction = {
                run_interaction = {
                    interaction = wc_magic_druid_forms_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                    execute_threshold = accept
                }
            }

            flag:wc_magic_summon_demon_interaction = {
                run_interaction = {
                    interaction = wc_magic_summon_demon_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                    execute_threshold = accept
                }
            }
        }
    }
    else_if = {
        limit = {
            is_ai = no
            has_variable = current_secondary_interaction_target
        }
        switch = {
            trigger = var:current_secondary_interaction

            flag:wc_magic_fire_shield_interaction = {
                open_interaction_window = {
                    interaction = wc_magic_fire_shield_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                }
            }

            flag:wc_magic_give_disease_interaction = {
                open_interaction_window = {
                    interaction = wc_magic_give_disease_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                }
            }

            flag:wc_magic_epidemic_interaction = {
                open_interaction_window = {
                    interaction = wc_magic_epidemic_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                }
            }

            flag:wc_magic_druid_forms_interaction = {
                open_interaction_window = {
                    interaction = wc_magic_druid_forms_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                }
            }

            flag:wc_magic_summon_demon_interaction = {
                open_interaction_window = {
                    interaction = wc_magic_summon_demon_interaction
                    actor = var:real_caster
                    recipient = var:current_secondary_interaction_target
                }
            }
        }
    }
}

cast_spell_effect_ai = { # Stage 8
    if = {
        limit = {
            can_cast_selected_spell_trigger = yes
        }
        cast_spell_sgui_effect = yes
        if = {
            limit = {
                debug_only = yes
            }
            change_global_variable = {
                name = ai_spells_pre_cast_stage_8
                add = 1
            }
        }
        trigger_event = {
            on_action = wc_on_ai_spellcasting
            days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
        } 
    }
    else = {
        trigger_event = {
            on_action = wc_on_ai_spellcasting
            days = { minimum_days_before_casting_value maximum_days_before_casting_value } # So it will not cause a major lag on every 1st of each month
        } 
    }
    remove_variable = current_secondary_interaction
    remove_variable = current_secondary_interaction_target
    remove_magic_variables = yes
}

remove_magic_variables = {
    # Remove all spell-related variables
    remove_variable = current_spell_name
    remove_variable = current_spell_rank
    remove_variable = current_spell_mana_cost
    remove_variable = current_spell_class
    remove_variable = known_spells
    remove_variable = casting_on_behalf

    remove_variable = current_target_type
    remove_variable = known_spells_only

    remove_variable = elemental_type
    remove_variable = spellbook_open
    clear_variable_list = elemental_types
    clear_variable_list = spell_ranks
    clear_variable_list = current_spell_list

    remove_variable = spell_recipient
    remove_variable = original_recipient
    remove_variable = spellbook_type_cast
    remove_variable = spellbook_type_view
    remove_variable = spell_targets_list
    remove_variable = previous_target
    clear_variable_list = target_titles


    remove_variable = spellbook_open
    remove_variable = direct_only
    remove_variable = eligible_only
}

ai_check_channeled_spells_effect = {
    if = {
        limit = {
            any_in_list = {
                variable = current_spells_channeling
                count >= 1
            }
        }
        random_list = {
            90 = {  }
            10 = {
                modifier = {
                    add = 20
                    var:wc_current_mana_variable <= wc_twenty_percent_of_max_mana_value
                }
                modifier = {
                    add = 20
                    var:wc_current_mana_variable <= wc_thirty_percent_of_max_mana_value
                }
                modifier = {
                    add = 10
                    var:wc_current_mana_variable <= wc_fourty_percent_of_max_mana_value
                }
                modifier = {
                    add = 10
                    var:wc_current_mana_variable <= wc_sixty_percent_of_max_mana_value
                }
                random_in_list = {
                    variable = current_spells_channeling
                    save_scope_as = this_spell
                    execute_scoped_spell_effect = {
                        SPELL = scope:this_spell
                        EFFECT = cancel_cast_effect
                    }
                }
            }
        }
    }
}

# TODO_MAGIC_AI: Exceptions for Raise dead?