# Determine whether Blackhand exists or not -- SS NOTE: THATS NOT WHAT THIS EVEN DOES
set_horde_characters_effect = {
	# TODO: Possibly set up this effect to find suitable alternatives to story characters
	title:c_dark_portal.title_province = {
		save_scope_as = horde_entry_point
	}
	# If started in 583, give Blackhand resources and set major players
	story_owner = {
		save_scope_as = blackhand_character
	}
	character:10005 = { 
		save_scope_as = kilrogg_character
	}
	character:10015 = {
		save_scope_as = guldan_character
	}
	# character:10017 = { Not used
	# 	save_scope_as = teron_character
	# }
	character:10019 = {
		save_scope_as = durotan_character
	}
	character:10050 = {
		save_scope_as = orgrim_character
	}
	character:52000 = {
		save_scope_as = chogall_character
	}
	character:5000 = { 
		save_scope_as = medivh_character
	}
	character:10019 = { 
		save_scope_as = durotan_character
	}
	story_owner = {
		add_gold = 250
		add_dread = high_dread
		add_prestige = 750
		dynasty = {
			add_dynasty_prestige_level = 3
			add_dynasty_prestige = 3000
			add_dynasty_perk = warfare_legacy_1
			add_dynasty_perk = warfare_legacy_2
		}
		# Don't spawn armies here, it doesn't work
	}
}

get_story_character = { # takes one variable, NAME, a saved character variable to get
	scope:story = { 
		var:$NAME$_character = { 
			save_scope_as = $NAME$_character
		}
	}
}

durotan_blackhand_intro_setup_effect = { 

		# Durotan Introduction
		set_variable = { 
			name = durotan_character
			value = scope:durotan_character
		}

		scope:durotan_character = {
			random = { 
				chance = 95
				add_character_flag = keeps_old_ways
			}
			trigger_event = {
				id = wc_horde_invasion.1521
				days = 7
			}
		}

		# Orgrim Introduction
		set_variable = { 
			name = orgrim_character
			value = scope:orgrim_character
		}

		scope:orgrim_character = {
			random = { 
				chance = 5
				add_character_flag = keeps_old_ways
			}
			trigger_event = {
				id = wc_horde_invasion.1531
				days = 7
			}
		}
}

setup_invasion_story_control_flags_effect = { 
	if = { 
		limit = { game_start_date <= 583.1.1 }
		set_variable = { 
			name = horde_invasion_story_phase
			value = flag:first_war
		}
	}
	# add other possible start conditions for the story here 
}

# Give Horde troops
spawn_horde_troops_effect = {
		spawn_army = {
			uses_supply = no
			inheritable = no
			name = horde_event_troops
			levies = {
				value = 1000
			}
			men_at_arms = {
				type = savage
				stacks = 1
			}
			men_at_arms = {
				type = blackrock_legionnaire
				stacks = 1
			}
			men_at_arms = {
				type = wolfrider
				stacks = 2
			}
			men_at_arms = {
				type = trebuchet
				stacks = 5
			}
			location = capital_province
		}
}

spawn_orc_savage_troops_effect = {
		spawn_army = {
			uses_supply = no
			inheritable = no
			name = horde_event_troops
			levies = {
				value = 1000
			}
			men_at_arms = {
				type = savage
				stacks = 3
			}
			men_at_arms = {
				type = wolfrider
				stacks = 2
			}
			men_at_arms = {
				type = trebuchet
				stacks = 5
			}
			location = capital_province
		}
}

spawn_orc_blademaster_troops_effect = {
		spawn_army = {
			uses_supply = no
			inheritable = no
			name = horde_event_troops
			levies = {
				value = 1000
			}
			men_at_arms = {
				type = blademaster
				stacks = 3
			}
			men_at_arms = {
				type = wolfrider
				stacks = 2
			}
			men_at_arms = {
				type = trebuchet
				stacks = 5
			}
			location = capital_province
		}
}

spawn_orc_troops_based_on_culture_effect = {
	if = { 
		limit = { 
			exists = capital_province
			realm_size < 80 
		}
		if = {
			limit = { culture = { has_innovation = innovation_savage } }
			spawn_orc_savage_troops_effect = yes
		}
		else_if = {
			limit = { culture = { has_innovation = innovation_blademaster } }
			spawn_orc_blademaster_troops_effect = yes
		}
		else = {
			spawn_horde_troops_effect = yes
		}
	}
}

# Declare war on Stormwind
declare_war_on_stormwind_effect = {
	debug_log = "The Horde has declared war on Stormwind!"
	debug_log_date = yes
	
	title:k_stormwind.holder = { save_scope_as = stormwind_holder }
	
	scope:warchief = {
		add_character_flag = {
			flag = free_mongol_cb
			days = 14
		}
		start_war = {
			cb = mongol_invasion_war
			target = title:k_stormwind.holder
			target_title = title:k_stormwind
		}
	}
	scope:story = { 
		set_variable = { 
			name = first_war_target
			value = scope:stormwind_holder
		}
	}
	title:k_shadow_council.holder = { 
		trigger_event = { 
			id = wc_horde_invasion.1512
			days = 14
		}
	}
}

try_to_set_horde_story_owner = {
	if = {
		limit = { $OWNER$ = { culture = { has_cultural_pillar = heritage_orcish } } }
		$OWNER$ = {
			add_character_modifier = { modifier = great_invader_modifier }
			trigger_event = { id = wc_horde_invasion.0004 days = 1 } #Spawns Horde troops

			# Truce on Blackrock Mountain
			title:c_blackrock_depths.holder = { save_scope_as = blackrock_mountain_holder }

			if = {
				limit = { scope:blackrock_mountain_holder = { has_variable = blackrock_mountain_truce_active_var } }
				trigger_event = {
					id = wc_horde_invasion.3010
					days = { 10 20 }
				}
			}
		}
		make_story_owner = $OWNER$
	}
	else = {
		end_story = yes
	}
}

get_random_county_effect = {
	save_temporary_scope_as = target
	create_title_and_vassal_change = {
		type = granted
		save_scope_as = title_granting
		add_claim_on_loss = no
	}
	$GIVER$ = {
		random_held_title = {
			limit = {
				tier = tier_county
				NOT = { this = $GIVER$.capital_county }
			}
			change_title_holder = {
				holder = scope:target
				change = scope:title_granting
			}
		}
	}
	resolve_title_and_vassal_change = scope:title_granting
}

horde_clan_start_pack_effect = {
	add_gold = 250
	spawn_orc_troops_based_on_culture_effect = yes
	spawn_orc_troops_based_on_culture_effect = yes
}

# Make clan a tribal and assign laws
clan_setup_effect = {
	change_government = tribal_government
	$clan$ = {
		if = {
			limit = { tier >= tier_kingdom }
			add_title_law = saxon_elective_succession_law
		}
	}
}

### WAR EFFECTS ###

take_control_horde_effect = {
	save_temporary_scope_as = target
	$PREVIOUS_HOLDER$ = { save_temporary_scope_as = previous_holder }
	$NEW_TITLE$ = { save_temporary_scope_as = conquered_title }
	
	create_title_and_vassal_change = {
		type = conquest
		save_scope_as = title_conquest
		add_claim_on_loss = yes
	}
	
	scope:previous_holder = {
		scope:conquered_title = {
			change_title_holder = {
				holder = scope:target
				change = scope:title_conquest
				take_baronies = yes
			}
		}
	}
	resolve_title_and_vassal_change = scope:title_conquest

	scope:conquered_title = {
		every_county_province = {
			barony = {
				if = {
					limit = {
						exists = lessee
					}
					revoke_lease = yes
				}
			}
		}
	}
}

change_culture_and_faith_horde_area_effect = {
	$TITLE$ = { save_scope_as = target_title }
	$CHARACTER$ = { save_scope_as = target_character }
	
	
	scope:target_title = { #Only for duchies
		every_de_jure_county = {
			limit = {
				NOT = {
					this.culture = scope:target_character.culture
					this.faith = scope:target_character.faith
				}
			}
			set_county_culture = scope:target_character.culture
			set_county_faith = scope:target_character.faith
		}
	}
}

change_culture_and_faith_horde_effect = {
	save_temporary_scope_as = target
	
	scope:conquered_title = {
		set_county_culture = scope:target.culture
		set_county_faith = scope:target.faith
	}
}

sack_area_effect = {
	$TITLE$ = { save_scope_as = target_title }
	scope:target_title = { #Only for duchies
		every_de_jure_county = {
			limit = {
				NOT = { this = { has_county_modifier = recently_sacked_by_the_horde_modifier } }
			}
			sack_county_effect = { LOCATION = this }
		}
	}
}

sack_county_effect = {
	$LOCATION$ = { save_scope_as = sacked_location }

	root = { add_gold = massive_gold_value }
	scope:sacked_location = {
		change_development_level = { # Lower Dev Level
			value = {
				add = development_level
				multiply = { -0.8 -0.9 }
			}
		}
		
		change_county_control = { # Lower control
			value = { add = monumental_county_control_loss }
		}
		
		add_county_modifier = {
			modifier = recently_sacked_by_the_horde_modifier
			years = 20
		}
		
		every_county_province = {
			if = {
				limit = { # Remove all of the buildings
					NOT = { has_holding_type = tribal_holding }
				}
				set_holding_type = tribal_holding
			}
			
			barony = {
				if = {
					limit = {
						exists = lessee
					}
					revoke_lease = yes
				}
			}
		}
	}
}

minor_sack_area_effect = {
	$TITLE$ = { save_scope_as = target_title }
	scope:target_title = { #Only for duchies
		every_de_jure_county = {
			limit = {
				NOT = { this = { has_county_modifier = recently_sacked_by_the_horde_modifier } }
			}
			minor_sack_county_effect = { LOCATION = this }
		}
	}
}

minor_sack_county_effect = {
	$LOCATION$ = { save_scope_as = sacked_location }

	root = { add_gold = minor_gold_value }
	scope:sacked_location = {
		change_development_level = { # Lower Dev Level
			value = {
				add = development_level
				multiply = { -0.1 -0.2 }
			}
		}
		
		change_county_control = { # Lower control
			value = { add = medium_county_control_loss }
		}
		
		add_county_modifier = {
			modifier = recently_sacked_by_the_horde_modifier
			years = 5
		}
	}
}

horde_bloodshed_area_effect = {
	$TITLE$ = { save_scope_as = target_title }
	scope:target_title = { #Only for duchies
		every_de_jure_county = {
			limit = {
				NOT = { 
					this = { has_county_modifier = recently_sacked_by_the_horde_modifier }
					this.holder = { is_vassal_or_below_of = title:e_horde.holder } 
				}
			}
			horde_bloodshed_effect = { LOCATION = this }
		}
	}
}

horde_bloodshed_effect = {
	$LOCATION$ = { save_scope_as = bloodshed_location }
	set_variable = {
		name = victims_killed
		value = 0
	}

	set_variable = {
		name = victims_enslaved
		value = 0
	}
	
	every_hired_mercenary  = { #Gets mercenaries hired by the Player
		mercenary_company_leader = {
			every_courtier = {
				add_to_temporary_list = hired_characters
			}
			every_vassal_or_below = {
				add_to_temporary_list = hired_characters
			}
			add_to_temporary_list = hired_characters
		}
	}
	every_patroned_holy_order = {
		leader = {
			add_to_temporary_list = hired_characters
		}
	}
	every_vassal_or_below = {
		every_patroned_holy_order = {
			leader = {
				add_to_temporary_list = hired_characters
			}
		}
		every_courtier = {
			add_to_temporary_list = hired_characters
		}
	}


	scope:bloodshed_location = {
		hidden_effect = {
			every_county_province = {
				
				every_character_in_location = {
					limit = {
						NOR = {
							this = root
							is_vassal_or_below_of = root
							employer ?= root
							is_courtier_of = root
							is_in_list = hired_characters
						}
					}
					save_scope_as = victim
					
					random_list = {
						5 = {
							#Nothing
						}
						15 = { 
							# Slave
							prisoner_of_war_capture_effect = {
								TARGET = scope:victim
								IMPRISONER = root
							}
							root = {
								change_variable = {
									name = victims_enslaved
									add = 1
								}
							}
						}
						85 = {
							root = {
								change_variable = {
									name = victims_killed
									add = 1
								}
							}
							override_death_effect = { death_reason = death_horde }
						}
					}
				}
				if = {
					limit = { has_building_or_higher = northshire_abbey_01 }
					remove_building = northshire_abbey_01
				}
				
			}
		}
	}

	hidden_effect = { # Dont unhide this or the TOOLTIP will show 0 people killed
		send_interface_toast = {
			title = horde_bloodshed.t
			add_prestige = medium_prestige_gain
			left_icon = root
			if = {
				limit = {
					NAND = {
						exists = var:victims_enslaved
						exists = var:victims_killed
						var:victims_killed = 0
						var:victims_enslaved = 0
					}
				}
				custom_tooltip = horde_bloodshed.d
			}
		}
		remove_variable = victims_enslaved
		remove_variable = victims_killed	
	}
	
	show_as_tooltip = { custom_tooltip = horde_bloodshed.residents }
}

might_lose_garona_hook_effect = { # scope this to the story; takes one parameter CHANCE; percent chance to lose control of her
	if = { 
		limit = { 
			var:garona_character = { 
				exists = this 
				is_alive = yes
			}
			garona_is_mindcontrolled_trigger = yes
		} 
		random_list = { 
			100 = { 
				# nothing happens, mind control maintained
				modifier = { 
					add = { 
						value = $CHANCE$
						multiply = -1
						min = 1
					}
				}
			}
			0 = { 
				# garona breaks mind control
				modifier = { 
					add = { 
						value = $CHANCE$
						max = 99
					} 
				}
				var:guldan_character = {
					send_interface_toast = {
						type = event_toast_effect_bad
						title = garona_hook_lost
						remove_hook ={
							target = prev.var:garona_character
							type = mindcontrol_hook
						}
						remove_character_modifier = garonas_intelligence_modifier
					}
				}
			}
		}
	}
}

garona_breakout_effect = { # todo: add this to the normal breakout rules
	cp:councillor_spymaster = { save_scope_as = stormwind_spymaster }
	scope:garona_character = { 
		save_scope_as = garona_character
		duel = { 
			skill = intrigue
			target = scope:stormwind_spymaster
			40 = { # failure - garona cannot escape
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3
				}

				scope:story = { 
					might_lose_garona_hook_effect = { CHANCE = 25 }
				}
			}
			60 = { # success - garona escape
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3
				}
				modifier = { # guldan will use his powers to help her
					scope:story = { garona_is_mindcontrolled_trigger = yes }
					add = 10
				}
				create_character_memory = {
					type = escaped_from_prison_memory
					
					participants = {
						imprisoner = root
					}
				}
				release_from_prison = yes
				if = { 
					limit = {
						OR =  {
							AND = { 
								NOT = { scope:guldan_character = { has_strong_usable_hook = prev } }
								has_character_flag = tortured_at_stormwind
							}
							AND = { 
								scope:guldan_character = { 
									has_strong_usable_hook = prev 
									is_incapable_trigger = no
								}
								scope:medivh_character= { is_alive = no }
							}
						}
					}
					assassination_of_lane_effect = yes
				}
			}
		}
	}
}

assassination_of_lane_effect = { 
	get_story_character = { NAME = garona } # even tho we are in garona scope, this is now needed for 2901
	get_story_character = { NAME = guldan }
	scope:story.var:first_war_target = { save_scope_as = stormwind_king }
	if = { 
		limit = { 
			OR = { 
				scope:story = { garona_is_mindcontrolled_trigger = yes }
				scope:garona_character = { has_character_flag = tortured_at_stormwind }
			}
		}
		scope:stormwind_king = { 	# add the victim
			add_to_temporary_list = 2901_receivers
			every_vassal_or_below = { 			# his vassals should know what happened 
				add_to_temporary_list = 2901_receivers
			}
		}
		scope:story.story_owner = {				# horde leader should know
			add_to_temporary_list = 2901_receivers
			every_vassal_or_below = {			# his vassals should rejoice
				add_to_temporary_list = 2901_receivers
			}
			every_war_enemy = { 				# his enemies should quake in their boots
				limit = { is_in_coalition_against_trigger = { CRISIS_HOLDER = prev } }
				add_to_temporary_list = 2901_receivers
			}
		}
		scope:guldan_character = { 	# ensure guldan knows his plan went off
			save_scope_as = guldan_character
			add_to_temporary_list = 2901_receivers
		}
		every_in_list = { 
			list = 2901_receivers
			trigger_event = wc_horde_invasion.2901
		}
		scope:story = { 
			might_lose_garona_hook_effect = { CHANCE = 5 }
		}
		if = { 
			limit = { exists = current_travel_plan }
			current_travel_plan = { 
				cancel_travel_plan = yes
			}
		}
		remove_character_flag = assassination_ready
	}
}

get_moroes_character_effect = { 
	if = { 
		limit = {
			character:5806 = { 
				is_alive = yes
				employer = root
			}
		}
		character:5806 = { 
			save_scope_as = moroes_character
		}
	}
	else = { 
		if = { 
			limit = { 
				cp:councillor_steward = { 
					exists = yes
					NOT = { this = scope:khadgar_character }
				}
			}
			cp:councillor_steward = {
				save_scope_as = moroes_character
			}
		}
		else = { 
			ordered_courtier = { 
				limit = { can_be_steward_trigger = { COURT_OWNER = root } }
				order_by = { value = stewardship }
			}
			save_scope_as = moroes_character
		}
	}
}

medivh_death_effect = { 
	get_story_character = { NAME = garona }
	if = {
		limit = { exists = scope:moroes_character }
		give_land_to_settler = { TO_WHO = scope:moroes_character COUNTY_NAME = title:c_karazhan GIVE_INDEPENDENCY = yes ADD_CLAIM_ON_LOSS = no }
		give_land_to_settler = { TO_WHO = scope:moroes_character COUNTY_NAME = title:c_vice GIVE_INDEPENDENCY = yes ADD_CLAIM_ON_LOSS = no }
		give_land_to_settler = { TO_WHO = scope:moroes_character COUNTY_NAME = title:d_karazhan GIVE_INDEPENDENCY = yes ADD_CLAIM_ON_LOSS = no }
		give_land_to_settler = { TO_WHO = scope:moroes_character COUNTY_NAME = title:d_deadwind_pass GIVE_INDEPENDENCY = yes ADD_CLAIM_ON_LOSS = no }
		title:k_dalaran.holder = { # set medivh employer now so that he doesnt turn undead
			if = { 
				limit = { 
					faith.religion = { is_in_family = rf_light } 
				}
				add_courtier = scope:khadgar_character
			}
		}
		scope:moroes_character = { # turn courtiers undead
			add_to_list = undead_change
			every_courtier = { add_to_list = undead_change }
			every_vassal = { 
				add_to_list = undead_change
				every_courtier = { add_to_list = undead_change }
			}
			every_in_list = { 
				list = undead_change
				become_undead_no_notification_effect = yes
				set_culture = culture:scourge
				set_character_faith = faith:echo_of_life
			}
		}
		if = {
			limit = { 
				scope:medivh_character = {
					is_ai = no
					has_character_flag = should_play_as_moroes
				}
			}
			set_player_character = scope:moroes_character
		}
	}
	if = { 
		limit = { 
			NOT = { scope:medivh_character = { has_character_flag = warned_about_confrontation } }
			scope:guldan_character = { is_alive = yes }
		} 
		scope:guldan_character = {
			trigger_event = wc_horde_invasion.2111 # fall into torpor
		}
	}
	if = { 
		limit = { 
			scope:khadgar_character = { 
				is_alive = yes
				is_incapable_trigger = no
			} 
		}
		scope:medivh_character = {
			override_death_killer_effect = { 
				death_reason = death_battle
				killer = scope:khadgar_character
			}
		}
		set_location = title:c_dalaran.title_province # WTS port to dal, 33s
	}
	else_if = { 
		limit = { 
			scope:garona_character = { 
				is_alive = yes
				is_imprisoned = no
				is_incapable_trigger = no
			} 
		}
		scope:medivh_character = {
			override_death_killer_effect = { 
				death_reason = death_battle
				killer = scope:garona_character
			}
		}
	}
	else = { 
		scope:medivh_character = {
			override_death_effect = { 
				death_reason = death_battle
			}
		}
	}
	scope:garona_character = { 
		if = { 
			limit = {  
				is_alive = yes 
				scope:story.var:first_war_target = { 
					is_alive = yes 
					is_landed = yes
				}
			}
			scope:story.var:first_war_target = { save_scope_as = stormwind_king }
			if = { 
				limit = { is_imprisoned = yes } 
				scope:story = { garona_breakout_effect = yes }
			}
			else_if = { 
				limit = { 
					is_available = yes
					NOT = { location = scope:stormwind_king.capital_county.title_province }
				 }
				start_travel_plan = { 
					destination = scope:stormwind_king.capital_county.title_province
					on_arrival_on_action = garona_assassinate_lane_on_action
					on_arrival_destinations = all
					return_trip = yes
				}
			}
			else = { 
				add_character_flag = assassination_ready
			}
		}
	}
}

become_landless_medivh = {
    save_scope_as = adventurer
    if = {
        limit = {
            player_heir ?= {
                this != root
                is_adult = yes
            }
        }
        player_heir = {
            save_scope_as = laamp_heir
            save_scope_as = laamp_inheritor
            custom_tooltip = become_landless_adventurer_decision_title_scope_tt
        }
    }
    else_if = {
        limit = { top_liege != this }
        liege = {
            save_scope_as = laamp_liege
            save_scope_as = laamp_inheritor
            custom_tooltip = become_landless_adventurer_decision_title_scope_tt
        }
    }
    else = {
        custom_tooltip = become_landless_adventurer_decision_title_created_tt
        if = {
            limit = {
                NOT = { exists = scope:laamp_inheritor }
            }
            hidden_effect_new_object = {
                create_character = {
                    template = laamp_inheritor_template
                    culture = root.capital_county.culture
                    faith = root.capital_county.faith
                    dynasty = generate
                    location = root.capital_province
                    after_creation = {
                        save_scope_as = laamp_inheritor
                        add_character_flag = new_laamp_inheritor
                    }
                }
            }
        }
    }
    create_landless_adventurer_title_tooltip_effect = yes
    show_as_tooltip = {
        add_prestige = medium_prestige_gain
        add_character_modifier = {
            modifier = ep3_voluntary_laamp_character_modifier
            years = 5
        }
    }
    trigger_event = { id = ep3_laamps.0030 }
    # Debug tracking
    if = {
        limit = {
            is_ai = yes
            debug_only = yes
        }
        if = {
            limit = { exists = global_var:ai_voluntary_laamp_count }
            change_global_variable = {
                name = ai_voluntary_laamp_count
                add = 1
            }
        }
        else = {
            set_global_variable = {
                name = ai_voluntary_laamp_count
                value = 1
            }
        }
        debug_log = "AI became adventurer voluntarily"
        debug_log_scopes = yes
    }
    stress_impact = {
        craven = minor_stress_impact_gain
        content = minor_stress_impact_gain
        lazy = minor_stress_impact_gain
    }
}

# happens after the makgora duel ends
blackdoom_makgora_outcome_effect = { 
	if = { 
		limit = { scope:sc_victor = scope:orgrim_character }
		# add doomhammer 
		scope:orgrim_character = {
			add_to_temporary_list = 2902_recipients
		}
		# add blackhand
		scope:blackhand_character = {
			add_to_temporary_list = 2902_recipients
		}
		every_in_list = {
			list = 2902_recipients 
			trigger_event = wc_horde_invasion.2902
		}
	}
	else = { # otherwise give blackhand a little reward and let him know he's now off the lore path
		scope:blackhand_character = { 
			send_interface_toast = {
				type = event_toast_effect_good
				title = defended_title_makgora
				left_icon = this
				right_icon = scope:orgrim_character
				scope:orgrim_character = {
					override_death_killer_effect = { 
						death_reason = death_duel
						killer = prev
					}
				}
				add_legitimacy = {
					value = major_legitimacy_gain
		 			multiply = primary_title.tier
				}
				add_prestige = medium_prestige_gain
				following_alternate_path_effect = yes
			}
		}
	}
}

# happens when the blackhand-doomhammer makgora goes according to lore
blackdoom_makgora_lore_victory_effect = { 

	blackdoom_stormwind_title_change_effect = yes

	blackdoom_vassal_fealty_change_effect = yes

	blackdoom_horde_title_change_effect = yes

	trigger_event = { 
		days = 3
		on_action = blackhand_death_notification_on_action
	}

	scope:blackhand_character = {
		every_character_war = { 
			limit = {
				primary_attacker = this
				using_cb = mongol_invasion_war
			}
			end_war = invalidated
		}
		override_death_killer_effect = { 
			death_reason = death_duel
			killer = scope:orgrim_character
		}
	}
}

# add stormwind stuff first to set up blackhand for success and to accept vassals
blackdoom_stormwind_title_change_effect = { 
	create_title_and_vassal_change = {
		type = granted
		save_scope_as = change
		add_claim_on_loss = no
	}
	title:d_stormwind = { 
		if = { 
			limit = { 
				OR = { 
					holder = scope:blackhand_character 
					holder.top_liege = scope:blackhand_character
				}
			}
			add_to_temporary_list = titles_taken
		}
		every_in_de_jure_hierarchy = {
			limit = { 
				tier = tier_county
				holder.top_liege = scope:blackhand_character
			}
			add_to_temporary_list = titles_taken
		}
	}

	# and kingdom of Stormwind
	title:k_stormwind = { 
		add_to_temporary_list = titles_taken
	}

	every_in_list = {
		list = titles_taken
		change_title_holder = { 
				holder = scope:orgrim_character
				change = scope:change
		}
	}

	resolve_title_and_vassal_change = scope:change
}

# need to independently change the vassals over because otherwise shit is stupid
blackdoom_vassal_fealty_change_effect = { 
	create_title_and_vassal_change = {
		type = swear_fealty
		save_scope_as = change
		add_claim_on_loss = no
	}

	scope:blackhand_character = {
		every_vassal = {
			limit = { 
				NOT = {  
					any_held_title = { is_orcish_clan_title_trigger = yes }
					any_held_title = { this = title:k_shadow_council }
					this = scope:orgrim_character
				} 
			}
			change_liege = {
				liege = scope:orgrim_character
				change = scope:change
			}
		}
	}

	resolve_title_and_vassal_change = scope:change
}

# afterwards, the e_horde and k_stormwind titles are changed
blackdoom_horde_title_change_effect = { 
	
	create_title_and_vassal_change = {
		type = usurped
		save_scope_as = change
		add_claim_on_loss = yes
	}
	# doomhammer gets the horde
	title:e_horde = { 
		add_to_temporary_list = titles_taken
	}
	
	# and becomes leader of blackrock clan
	title:k_blackrock_clan ={ 
		add_to_temporary_list = titles_taken
	}

	every_in_list = {
		list = titles_taken
		change_title_holder = { 
				holder = scope:orgrim_character
				change = scope:change
		}
	}

	resolve_title_and_vassal_change = scope:change
}

blackdoom_makgora_alt_victory_effect = { 
	create_title_and_vassal_change = {
		type = usurped
		save_scope_as = change
		add_claim_on_loss = yes
	}

	scope:orgrim_character = {
		every_held_title = {
			add_to_temporary_list = titles_taken
		}
	}

	every_in_list = {
		list = titles_taken
		change_title_holder_include_vassals = {
			holder = scope:blackhand_character
			change = scope:change
		}
	}

	resolve_title_and_vassal_change = scope:change

	scope:orgrim_character = {
		override_death_killer_effect = { 
			death_reason = death_duel
			killer = scope:blackhand_character
		}
	}
}

test_WCHI_2000_effect = { 
	character:10000 = { 
		random_owned_story = {
			limit = { type = story_horde_invasion }
			save_scope_as = story
		}
	}
	trigger_event = wc_horde_invasion.2000
}

#################################
# Shadow Council Effects
#################################

skirmishes_in_blackrock_mountain_effect = {
	custom_tooltip = skirmishes_in_blackrock_mountain_effect.tt

	hidden_effect = {
		if = {
			limit = {
				exists = scope:skirmish_attacker
				exists = scope:skirmish_defender
				exists = scope:skirmish_attacker_liege
				exists = scope:skirmish_defender_liege
			}

			# Skirmish
			scope:skirmish_attacker = {
				duel = {
					skill = prowess
					target = scope:skirmish_defender

					18 = { # Attacker wins
						compare_modifier = {
							value = scope:duel_value
							multiplier = 1
							min = -15
						}
						scope:skirmish_attacker = { save_scope_as = skirmish_winner }
						scope:skirmish_defender = { save_scope_as = skirmish_loser }
						scope:skirmish_attacker_liege = { save_scope_as = skirmish_winner_liege }
						scope:skirmish_defender_liege = { save_scope_as = skirmish_loser_liege }
					}
					22 = { # Defender wins
						compare_modifier = {
							value = scope:duel_value
							multiplier = -1
							min = -15
						}
						scope:skirmish_defender = { save_scope_as = skirmish_winner }
						scope:skirmish_attacker = { save_scope_as = skirmish_loser }
						scope:skirmish_defender_liege = { save_scope_as = skirmish_winner_liege }
						scope:skirmish_attacker_liege = { save_scope_as = skirmish_loser_liege }
					}
				}
			}

			scope:skirmish_loser = {
				random_list = {
					40 = { } # Shaken, but unhurt
					45 = {
						increase_wounds_effect = { REASON = fight }

						# For the loser's own liege
						scope:skirmish_loser_liege = {
							send_interface_message = {
								type = event_slain_combatants_bad
								title = skirmishes_in_blackrock_mountain_effect.wounded_message.t
								desc = skirmishes_in_blackrock_mountain_effect.wounded_message.desc
								left_icon = scope:skirmish_loser
								right_icon = scope:skirmish_winner
							}
						}

						# For the enemy's liege
						scope:skirmish_winner_liege = {
							send_interface_message = {
								type = event_slain_combatants_good
								title = skirmishes_in_blackrock_mountain_effect.wounded_message.t
								desc = skirmishes_in_blackrock_mountain_effect.wounded_message.desc
								left_icon = scope:skirmish_loser
								right_icon = scope:skirmish_winner
							}
						}
					}
					15 = { # A killing blow
						override_death_killer_effect = {
							death_reason = death_battle
							killer = scope:skirmish_winner
						}

						# For the loser's own liege
						scope:skirmish_loser_liege = {
							send_interface_message = {
								type = event_slain_combatants_bad
								title = skirmishes_in_blackrock_mountain_effect.slain_message.t
								desc = skirmishes_in_blackrock_mountain_effect.slain_message.desc
								left_icon = scope:skirmish_loser
								right_icon = scope:skirmish_winner
							}
						}

						# For the enemy's liege
						scope:skirmish_winner_liege = {
							send_interface_message = {
								type = event_slain_combatants_good
								title = skirmishes_in_blackrock_mountain_effect.slain_message.t
								desc = skirmishes_in_blackrock_mountain_effect.slain_message.desc
								left_icon = scope:skirmish_loser
								right_icon = scope:skirmish_winner
							}
						}
					}
				}
			}
		}
	}
}

negotiate_blackrock_mountain_duel_effect = {
	scope:negotiator = {
		duel = {
			skill = diplomacy
			target = scope:blackrock_mountain_holder

			99 = { # The negotiator wins them over
				desc = negotiate_blackrock_mountain_duel_effect.success

				compare_modifier = {
					value = scope:duel_value
					multiplier = 1
					min = -15
				}
				modifier = { # Well-read negotiators argue more persuasively
					add = scope:negotiator.learning
					factor = 0.5
				}
				modifier = { # A fellow servant of the Firelord is trusted here
					add = 15
					scope:negotiator = {
						OR = {
							has_religion = religion:ignism_religion
							AND = {
								exists = secret_faith
								secret_faith = { religion = religion:ignism_religion }
							}
						}
					}
				}
				modifier = { # The Old Gods' whispers make for a very convincing tongue
					add = 15
					scope:negotiator = {
						has_faith = faith:twilights_hammer_cult
					}
				}

				# Accept
				scope:shadow_council_master = {
					show_as_tooltip = { negotiate_blackrock_mountain_accept_effect = yes }
					trigger_event = {
						id = wc_horde_invasion.3005
						days = { 10 14 }
					}
				}
			}
			1 = { # The talks collapse
				desc = negotiate_blackrock_mountain_duel_effect.failure

				compare_modifier = {
					value = scope:duel_value
					multiplier = -1
					min = -15
				}

				if = {
					limit = {
						exists = scope:skirmish_attacker
						exists = scope:skirmish_defender
					}
					skirmishes_in_blackrock_mountain_effect = yes # Actual fight
				}

				# Rejection + War
				negotiate_blackrock_mountain_reject_effect = yes
				show_as_tooltip = { declare_war_on_blackrock_holder_effect = yes }
				scope:shadow_council_master = {
					trigger_event = {
						id = wc_horde_invasion.3006
						days = { 10 14 }
					}
				}
			}
		}
	}
}

negotiate_blackrock_mountain_accept_effect = {
	custom_tooltip = negotiate_blackrock_mountain_accept_effect.partition_active.tt

	take_blackrock_spire_effect = yes
	take_burning_steppes_effect = yes

	# Truce with the Shadow Council, vassals and the Horde
	scope:blackrock_mountain_holder = {
		# Truce variable
		set_variable = {
			name = blackrock_mountain_truce_active_var
			years = 15
		}

		# Shadow Council
		add_truce_both_ways = {
			character = scope:shadow_council_master
			years = 15
			name = TRUCE_SIGNED_PARTITION
		}

		# Vassals
		scope:shadow_council_master = {
			every_vassal = {
				scope:blackrock_mountain_holder = {
					add_truce_both_ways = {
						character = prev
						years = 15
						name = TRUCE_SIGNED_PARTITION
					}
				}
			}
		}

		# Horde
		add_truce_both_ways = {
			character = scope:shadow_council_master.top_liege
			years = 15
			name = TRUCE_SIGNED_PARTITION
		}
	}

	# The Shadow Council Master values the negotiator's loyalty
	scope:shadow_council_master = {
		add_opinion = {
			target = scope:negotiator
			modifier = loyal_servant
			opinion = 30
		}
	}
}

negotiate_blackrock_mountain_reject_effect = {
	scope:negotiator = {
		if = {
			limit = {
				can_set_relation_nemesis_trigger = { CHARACTER = scope:blackrock_mountain_holder }
			}
			set_relation_nemesis = {
				reason = nemesis_blackrock_mountain_rejection
				copy_reason = rival
				target = scope:blackrock_mountain_holder
			}
		}
		else = {
			add_opinion = {
				target = scope:blackrock_mountain_holder
				modifier = humiliated_opinion
				opinion = -75
			}
		}
		add_prestige = massive_prestige_loss
		give_nickname = nick_the_weak
	}
}

take_blackrock_spire_effect = {
	custom_tooltip = {
		text = take_blackrock_spire_effect.tt

		create_title_and_vassal_change = {
			type = granted
			save_scope_as = change
		}

		title:c_blackrock_spire = { # TODO: After map update, be sure it still work
			change_title_holder = {
				holder = scope:shadow_council_master
				change = scope:change
				take_baronies = yes
			}

			# Change culture and faith
			set_county_culture = scope:shadow_council_master.top_liege.culture
			set_county_faith = scope:shadow_council_master.faith
			change_development_level = { # Reset development after the takeover
				value = {
					add = development_level
					multiply = { -0.8 -0.9 }
				}
			}
		}

		resolve_title_and_vassal_change = scope:change

		scope:shadow_council_master = {
			set_realm_capital = title:c_blackrock_spire
		}
	}
}

take_burning_steppes_effect = {
	custom_tooltip = {
		text = take_burning_steppes_effect.tt

		create_title_and_vassal_change = {
			type = granted
			save_scope_as = change
			add_claim_on_loss = no
		}

		# Every county in the Burning Steppes held by the Blackrock Mountain holder or one of their vassals
		title:d_redhelm = { # TODO: After map update, be sure it still work
			every_de_jure_county = {
				limit = {
					exists = holder
					holder = {
						OR = {
							this = scope:blackrock_mountain_holder
							is_vassal_or_below_of = scope:blackrock_mountain_holder
						}
					}
				}
				change_title_holder = {
					holder = scope:shadow_council_master
					change = scope:change
					take_baronies = yes
				}

				# Change culture and faith
				set_county_culture = scope:shadow_council_master.top_liege.culture
				set_county_faith = scope:shadow_council_master.faith
				change_development_level = { # Reset development after the takeover
					value = {
						add = development_level
						multiply = { -0.8 -0.9 }
					}
				}
			}
		}
		title:d_burning_steppes = { # TODO: After map update, be sure it still work
			every_de_jure_county = {
				limit = {
					exists = holder
					holder = {
						OR = {
							this = scope:blackrock_mountain_holder
							is_vassal_or_below_of = scope:blackrock_mountain_holder
						}
					}
				}
				change_title_holder = {
					holder = scope:shadow_council_master
					change = scope:change
					take_baronies = yes
				}

				# Change culture and faith
				set_county_culture = scope:shadow_council_master.top_liege.culture
				set_county_faith = scope:shadow_council_master.faith
				change_development_level = { # Reset development after the takeover
					value = {
						add = development_level
						multiply = { -0.8 -0.9 }
					}
				}
			}
		}

		resolve_title_and_vassal_change = scope:change
	}
}

take_badlands_effect = {
	custom_tooltip = {
		text = take_badlands_effect.tt

		create_title_and_vassal_change = {
			type = granted
			save_scope_as = change
			add_claim_on_loss = no
		}

		# Every county in the Badlands held by the Blackrock Mountain holder or one of their vassals
		title:d_badland = { # TODO: After map update, be sure it still work
			every_de_jure_county = {
				limit = {
					exists = holder
					holder = {
						OR = {
							this = scope:blackrock_mountain_holder
							is_vassal_or_below_of = scope:blackrock_mountain_holder
						}
					}
				}
				change_title_holder = {
					holder = scope:negotiator
					change = scope:change
					take_baronies = yes
				}

				# Change culture and faith
				set_county_culture = scope:negotiator.culture
				set_county_faith = scope:negotiator.faith
				change_development_level = { # Reset development after the takeover
					value = {
						add = development_level
						multiply = { -0.8 -0.9 }
					}
				}
			}
		}
		title:d_lethlor = { # TODO: After map update, be sure it still work
			every_de_jure_county = {
				limit = {
					exists = holder
					holder = {
						OR = {
							this = scope:blackrock_mountain_holder
							is_vassal_or_below_of = scope:blackrock_mountain_holder
						}
					}
				}
				change_title_holder = {
					holder = scope:negotiator
					change = scope:change
					take_baronies = yes
				}

				# Change culture and faith
				set_county_culture = scope:negotiator.culture
				set_county_faith = scope:negotiator.faith
				change_development_level = { # Reset development after the takeover
					value = {
						add = development_level
						multiply = { -0.8 -0.9 }
					}
				}
			}
		}

		resolve_title_and_vassal_change = scope:change
	}
}

declare_war_on_blackrock_holder_effect = {
	scope:shadow_council_master = {
		add_character_flag = {
			flag = free_mongol_cb
			days = 14
		}
		start_war = {
			cb = mongol_invasion_war
			target = scope:blackrock_mountain_holder
			target_title = title:k_shadowforge
		}
	}
}

horde_invasion_first_war_end_effect = { 
	scope:story = { 
		remove_variable = horde_invasion_story_phase
		set_variable = { 
			name = horde_invasion_story_phase
			value = flag:interwar
		}
		story_owner = {
			add_character_flag = plotting_next_invasion
		}
	}
}
