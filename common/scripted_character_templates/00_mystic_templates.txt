default_mystic_character = {
	# Warcraft
	age = { age_30_value age_80_value }
	trait = mystic_1
	random_traits = yes
	faith = root.faith
	gender_female_chance = {
		if = {
			limit = { root.faith = { has_doctrine = doctrine_gender_male_dominated } }
			add = 0
		}
		else_if = {
			limit = { root.faith = { has_doctrine = doctrine_gender_female_dominated } }
			add = 100
		}
		else = {
			add = 50
		}
	}
	learning = max_template_high_skill
	
	after_creation = {
		# Warcraft
		try_to_set_race_effect = { FLAG = no_gene }			#Assigns race trait
	}
}