set_scripted_appearance_effect = {
	# We don't need it anymore since portrait modifiers with bigger weight have priority
	# add_character_flag = has_scripted_appearance
	
	# So it doesn't change the look
	# if = {
		# limit = { has_trait = beauty_good }
		# remove_beauty_good_group_effect = yes
		# add_trait = beauty_good_lore
	# }
}