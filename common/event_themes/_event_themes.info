event_theme_key = {
	background = {		# A background that can be shown when the event pops up. In case that there are multiples the first one that fits the trigger will be the one selected.
		trigger = {}	# Receives the event scope to check if it's valid.
		event_background = "" 	# Key to event backgrounds database
	}
	icon = {			# An icon that can be shown when the event pops up. In case that there are multiples the first one that fits the trigger will be the one selected.
		trigger = {}	# Receives the event scope to check if it's valid.
		reference = "" 	# Path to the texture
	}
	sound = {			# A sound that can be played when the event pops up. In case that there are multiples the first one that fits the trigger will be the one selected.
		trigger = {}	# Receives the event scope to check if it's valid.
		reference = "" 	# Reference of the sound
	}
}
