# The interval in months at which the profession learning should be checked by AI.
learn_profession_check_interval = 24

# Cost of learning a new profession. Equivalent to the hire physician cost.
learn_profession_gold_cost = {
	value = minor_gold_value
	#if = {
	#	limit = {
	#		has_perk = anatomical_studies_perk
	#	}
	#	multiply = 0.5
	#}
}

# Update
# Maybe tie this in with some cultures
profession_soft_cap = { # Update
	value = 1
	if = {
		limit = {
			learning >= language_learning_divide_value
		}
		add = {
			add = learning
			divide = language_learning_divide_value
		}
	}
	if = {
		limit = {
			has_perk = open_minded_perk
		}
		add = 2
	}
	if = {
		limit = {
			has_perk = smooth_operator_perk
		}
		add = 1
	}
	if = {
		limit = {
			exists = dynasty
			dynasty = {
				has_dynasty_perk = ep1_culture_legacy_1
			}
		}
		add = 1
	}
	if = {
		limit = {
			culture = {
				has_cultural_parameter = extra_language_cap
			}
		}
		add = 1
	}
	if = {
		limit = {
			culture = {
				has_cultural_parameter = extra_language_cap_2
			}
		}
		add = 1
	}
}

# Update
profession_soft_cap_modifier_value = {
	value = num_of_known_languages # Update
	subtract = profession_soft_cap
}