Difficulty information shown when selecting a character to play in the lobby.

== Structure ==

# Key is also used as localization key for the entry
psi_at_war = {
	# Trigger to control if the entry is shown for a character
	# Scope: root = character
	is_shown = {}
}
