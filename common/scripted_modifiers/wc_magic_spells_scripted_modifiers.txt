spellsteal_has_modifier_modifier = {
    modifier = {
        add = 1
        has_character_modifier = $MODIFIER$
        NOT = {
            root = {
                has_character_modifier = $MODIFIER$
            }
        }
    }
}