# Agent types for wc_mograine_assassination - open to any eligible courtier
# via agent_groups_owner_perspective/agent_join_chance on the scheme itself,
# same pattern as vanilla agent_assassin (no valid_agent_for_slot filter).
# Renault stays a scripted event participant instead of an agent slot since
# his own employer switch lands mid-story, too unreliable to gate a slot on.

wc_mograine_whitemane_agent = {
	contribution_type = success_chance_max

	contribution = {
		value = scheme_agent_general_bonuses_contribution_score_value

		add = {
			value = intrigue
			multiply = agent_3to1_1_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.intrigue
		}
		add = {
			value = learning
			multiply = agent_3to1_1_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.learning
		}

		if = {
			limit = { has_trait = zealous }
			add = {
				value = agent_trait_bonus_t3_value
				desc = scheme_agent_aptitude.trait.zealous
			}
		}
		if = {
			limit = { has_trait = wrathful }
			add = {
				value = agent_trait_bonus_t2_value
				desc = scheme_agent_aptitude.trait.wrathful
			}
		}
		if = {
			limit = { has_trait = arbitrary }
			add = {
				value = agent_trait_bonus_t1_value
				desc = scheme_agent_aptitude.trait.arbitrary
			}
		}
		if = {
			limit = { has_trait = compassionate }
			add = {
				value = agent_trait_malus_t3_value
				desc = scheme_agent_aptitude.trait.compassionate
			}
		}

		max = agent_scheme_contributor_max
		min = agent_scheme_contributor_min
	}
}

wc_mograine_voss_agent = {
	contribution_type = success_chance_max

	contribution = {
		value = scheme_agent_general_bonuses_contribution_score_value

		add = {
			value = intrigue
			multiply = agent_3to1_1_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.intrigue
		}
		add = {
			value = learning
			multiply = agent_3to1_1_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.learning
		}

		if = {
			limit = { has_trait = zealous }
			add = {
				value = agent_trait_bonus_t3_value
				desc = scheme_agent_aptitude.trait.zealous
			}
		}
		if = {
			limit = { has_trait = wrathful }
			add = {
				value = agent_trait_bonus_t2_value
				desc = scheme_agent_aptitude.trait.wrathful
			}
		}
		if = {
			limit = { has_trait = stubborn }
			add = {
				value = agent_trait_bonus_t1_value
				desc = scheme_agent_aptitude.trait.stubborn
			}
		}
		if = {
			limit = { has_trait = compassionate }
			add = {
				value = agent_trait_malus_t3_value
				desc = scheme_agent_aptitude.trait.compassionate
			}
		}

		max = agent_scheme_contributor_max
		min = agent_scheme_contributor_min
	}
}

# contribution_type = secrecy, not success_chance_max like the other two:
# every non-secrecy agent slot drains -10 secrecy. Diplomacy-weighted
# (mirrors vanilla agent_alibi) since a respected elder lending cover fits
# diplomacy better than intrigue.
wc_mograine_founders_agent = {
	contribution_type = secrecy

	contribution = {
		value = scheme_agent_general_bonuses_contribution_score_value

		add = {
			value = diplomacy
			multiply = agent_3to1_3_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.diplomacy
		}
		add = {
			value = learning
			multiply = agent_3to1_1_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.learning
		}

		if = {
			limit = { has_trait = zealous }
			add = {
				value = agent_trait_bonus_t3_value
				desc = scheme_agent_aptitude.trait.zealous
			}
		}
		if = {
			limit = { has_trait = wrathful }
			add = {
				value = agent_trait_bonus_t2_value
				desc = scheme_agent_aptitude.trait.wrathful
			}
		}
		if = {
			limit = { has_trait = compassionate }
			add = {
				value = agent_trait_malus_t3_value
				desc = scheme_agent_aptitude.trait.compassionate
			}
		}

		max = agent_scheme_contributor_max
		min = agent_scheme_contributor_min
	}
}

wc_mograine_enforcer_agent = {
	contribution_type = success_chance_max

	contribution = {
		value = scheme_agent_general_bonuses_contribution_score_value

		add = {
			value = prowess
			multiply = agent_3to1_3_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.prowess
		}
		add = {
			value = martial
			multiply = agent_3to1_1_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.martial
		}

		if = {
			limit = { has_trait = brave }
			add = {
				value = agent_trait_bonus_t2_value
				desc = scheme_agent_aptitude.trait.brave
			}
		}
		if = {
			limit = { has_trait = wrathful }
			add = {
				value = agent_trait_bonus_t2_value
				desc = scheme_agent_aptitude.trait.wrathful
			}
		}
		if = {
			limit = { has_trait = compassionate }
			add = {
				value = agent_trait_malus_t3_value
				desc = scheme_agent_aptitude.trait.compassionate
			}
		}

		max = agent_scheme_contributor_max
		min = agent_scheme_contributor_min
	}
}

# 2nd secrecy-type slot - 5 total slots means 3 non-secrecy ones draining -30
# by themselves, one secrecy contributor alone would struggle to offset it.
wc_mograine_watcher_agent = {
	contribution_type = secrecy

	contribution = {
		value = scheme_agent_general_bonuses_contribution_score_value

		add = {
			value = intrigue
			multiply = agent_3to1_3_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.intrigue
		}
		add = {
			value = diplomacy
			multiply = agent_3to1_1_skill_mult_value
			ceiling = yes
			max = agent_max_skill_value
			desc = scheme_agent_aptitude.skill.diplomacy
		}

		if = {
			limit = { has_trait = deceitful }
			add = {
				value = agent_trait_bonus_t3_value
				desc = scheme_agent_aptitude.trait.deceitful
			}
		}
		if = {
			limit = { has_trait = patient }
			add = {
				value = agent_trait_bonus_t2_value
				desc = scheme_agent_aptitude.trait.patient
			}
		}
		if = {
			limit = { has_trait = honest }
			add = {
				value = agent_trait_malus_t3_value
				desc = scheme_agent_aptitude.trait.honest
			}
		}

		max = agent_scheme_contributor_max
		min = agent_scheme_contributor_min
	}
}
