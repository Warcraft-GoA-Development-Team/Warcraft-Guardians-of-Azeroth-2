###################
# SCHEME VALUES	#
###################
odds_plague_scheme_misc_value = {
	add = {
		value = -17
		desc = scheme_odds.misc_modifiers
		if = {
			limit = {
				OR = {
					scope:target = {
						is_at_war_with = scope:owner
					}
					scope:target = {
						is_playable_character = no
						exists = liege
						liege = {
							is_at_war_with = scope:owner
						}
					}
					scope:target = {
						is_playable_character = yes
						exists = liege
						liege = {
							is_at_war_with = scope:owner
						}
					}
				}
			}
			add = -30
		}
		if = {
			limit = {
				scope:target = {
					has_perk = prepared_for_anything_perk
				}
			}
			add = -30
		}
		if = {
			limit = {
				scope:target = {
					has_trait = trusting
				}
			}
			add = 18
		}
	}
}