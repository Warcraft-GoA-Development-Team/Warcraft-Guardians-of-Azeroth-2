== Structure ==

slot_name = {
	# Trigger for whether a specific character is valid for this agent slot.
	# root - The Agent character
	# scope:owner - The owner of the scheme.
	# scope:target - The target of the scheme.
	valid_agent_for_slot = { ... } 

	contribution_type = secrecy/speed/success_chance # What part of a scheme the agent should contribute to.

	# A scripted value. The resulting number is what the agent will contribute to whatever's defined as contribution_type.
	# root - The Agent character
	# scope:owner - The owner of the scheme.
	# scope:scheme - The scheme
	# scope:target - The target of the scheme.
	contribution = { ... }
}
