wc_condense_mana_gui = { # 1st debug spell, also used to show/hide the entire interface
	scope = character
	is_shown = { wc_max_mana_value > 0 }
	is_valid = { }
	effect = { set_variable = { name = wc_current_mana_variable value = 0 } }
}