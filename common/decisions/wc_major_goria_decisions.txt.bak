# Warcraft

###DECISIONS LIST###
#decision_restore_the_gorian_empire

restore_gorian_empire_decision = {
	picture = "gfx/interface/illustrations/decisions/gorian_restoration.dds"  ##NEEDS TO BE ADDED
	major = yes
	ai_check_interval = 12
	desc = restore_gorian_empire_decision_desc
	selection_tooltip = restore_gorian_empire_decision_tooltip

	is_shown = {
		OR = {
			has_culture_group = culture_group:ogre_group
			has_culture = culture:moknathal
		}
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:restore_gorian_empire_decision
			}
		}
	}

	is_valid = {
		has_government = feudal_government
		prestige_level >= 4
		sub_realm_size >= 10
	}

	is_valid_showing_failures_only = {
		is_capable_adult = yes
		is_imprisoned = no
		is_independent_ruler = yes
	}
	cost = {					
		gold = 100
		prestige = 300
	}

	effect = { ##CHECK ALL IN THE EFFECT
		custom_tooltip = restore_gorian_empire_decision_effect_tooltip

		show_as_tooltip = { create_gorian_empire_scripted_effect = yes}

		#Can only be done once
		add_to_global_variable_list = {
			name = unavailable_unique_decisions
			target = flag:restore_gorian_empire_decision
		}
		set_global_variable = {
			name = restore_gorian_empire_decision
			value = scope:western_slav_uniter
		}

		#Events
		trigger_event = gorian_restoration.0001
		every_player = {
			trigger_event = gorian_restoration.0001
		}	

		ai_potential = {
			always = yes
		}

		ai_will_do = {
			base = 100
		}
	}
}
