balnazzar_move_against_alexandros_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	# Dathrohan is duchy-tier (Tyr's Hand) here - Order isn't seized until .1006.
	# 0 = skip check, matching become_legion_commander_decision's pattern.
	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 24
		kingdom = 0
		empire = 0
		hegemony = 0
	}

	desc = balnazzar_move_against_alexandros_decision_desc

	is_shown = {
		has_trait = nathrezim_disguise
		has_character_flag = balnazzar_kelthuzad_pact_sealed
		exists = character:60017 # Alexandros Mograine
		character:60017 = { is_alive = yes }
		# The scheme's own presence is the real signal it's already in motion - same check
		# balnazzar_press_renault_decision's is_shown already uses.
		NOT = {
			any_scheme = {
				type = wc_mograine_assassination
				count > 0
			}
		}
	}

	# ck3 bug: bare is_alive=yes on a directly-scoped character mis-renders in Requirements panel.
	# Wrap in custom_tooltip with an explicit line instead.
	is_valid = {
		custom_tooltip = {
			text = balnazzar_move_against_alexandros_decision_requirement_alive
			exists = character:60017
			character:60017 = { is_alive = yes }
			exists = character:60019 # Renault Mograine
			character:60019 = { is_alive = yes }
		}
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		custom_tooltip = balnazzar_move_against_alexandros_decision_effect_tooltip
		trigger_event = {
			id = wc_balnazzar_story.1004
			days = 3
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 50
	}
}

# Active lever for wc_mograine_assassination - the scheme itself has no agents
# to recruit, so this is the only way to directly push it besides waiting.
# Yearly cooldown via the flag below, no built-in decision cooldown field exists.
balnazzar_press_renault_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 12
		kingdom = 0
		empire = 0
		hegemony = 0
	}

	desc = balnazzar_press_renault_decision_desc

	is_shown = {
		any_scheme = {
			type = wc_mograine_assassination
			count > 0
		}
	}

	is_valid_showing_failures_only = {
		custom_description = {
			text = balnazzar_press_renault_decision.cooldown_desc
			NOT = { has_character_flag = balnazzar_pressed_renault_cooldown }
		}
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		every_scheme = {
			type = wc_mograine_assassination
			add_scheme_progress = 15
		}
		add_stress = medium_stress_gain
		add_character_flag = { flag = balnazzar_pressed_renault_cooldown months = 2 }
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 30
	}
}

# Active lever for wc_balnazzar_story.1018's loyalty roll - lets the player spend gold and
# the disguise's own gift for manipulation to shift the odds before the roll happens, instead
# of only ever finding out which way Renault broke after the fact. Only shown between .1006
# (Renault made Commander, scarlet_crusade_founder flag set) and .1018 actually resolving
# (balnazzar_renault_resolved flag, set in both of .1018's outcomes).
balnazzar_steady_renault_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 12
		kingdom = 0
		empire = 0
		hegemony = 0
	}

	desc = balnazzar_steady_renault_decision_desc

	is_shown = {
		has_trait = nathrezim_disguise
		exists = character:60019 # Renault Mograine
		character:60019 = {
			is_alive = yes
			has_character_flag = scarlet_crusade_founder
			NOT = { has_character_flag = balnazzar_renault_resolved }
		}
	}

	is_valid_showing_failures_only = {
		custom_description = {
			text = balnazzar_steady_renault_decision.cooldown_desc
			NOT = { has_character_flag = balnazzar_steadied_renault_cooldown }
		}
		gold >= 100
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		add_gold = -100
		add_stress = minor_stress_gain
		add_character_flag = balnazzar_renault_steadied
		add_character_flag = { flag = balnazzar_steadied_renault_cooldown years = 1 }
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 20
	}
}

# Leans into being "Saidan Dathrohan" in public - real legitimacy reward, real witness risk,
# repeatable but on a cooldown.
balnazzar_invoke_saidans_legacy_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 12
		kingdom = 0
		empire = 0
		hegemony = 0
	}

	desc = balnazzar_invoke_saidans_legacy_decision_desc

	is_shown = {
		has_trait = nathrezim_disguise
	}

	is_valid_showing_failures_only = {
		custom_description = {
			text = balnazzar_invoke_saidans_legacy_decision.cooldown_desc
			NOT = { has_character_flag = balnazzar_invoked_legacy_cooldown }
		}
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		add_piety = medium_piety_value
		add_prestige = medium_prestige_value
		add_character_modifier = {
			modifier = wc_saidans_legacy_invoked_modifier
			years = 3
		}
		add_stress = minor_stress_gain
		add_character_flag = { flag = balnazzar_invoked_legacy_cooldown years = 2 }

		hidden_effect = {
			# Standing on a dead man's reputation in public draws exactly the kind of
			# close attention the disguise can least afford - same witness pattern
			# used throughout this arc (.0102, contingency .4001).
			random = {
				chance = 25
				random_courtier = {
					limit = {
						is_alive = yes
						NOT = { has_trait = being_undead }
						NOT = { has_character_flag = balnazzar_noticed_something_wrong }
					}
					add_character_flag = balnazzar_noticed_something_wrong
					save_temporary_scope_as = balnazzar_witness

					trigger_event = {
						id = wc_balnazzar_story.0101
						days = { 10 30 }
					}
				}
			}
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 25
	}
}

# Grand Crusader's authority, spent on the realm rather than the man wearing it - vassals
# still holding the old faith are made to pay for the privilege of practicing it, or convert
# outright. Real gold/piety now, real vassal resentment (natural faction risk through opinion,
# not a scripted count-threshold) as the cost.
balnazzar_demand_purity_tribute_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 12
		kingdom = 12
		empire = 12
		hegemony = 12
	}

	desc = balnazzar_demand_purity_tribute_decision_desc

	is_shown = {
		has_trait = nathrezim_disguise
		any_vassal = {
			count > 0
			NOT = { faith = faith:scarletism }
		}
	}

	is_valid_showing_failures_only = {
		custom_description = {
			text = balnazzar_demand_purity_tribute_decision.cooldown_desc
			NOT = { has_character_flag = balnazzar_demanded_purity_tribute_cooldown }
		}
		is_imprisoned = no
		is_incapable_trigger = no
	}

	effect = {
		add_gold = medium_gold_value
		add_piety = minor_piety_value
		add_character_flag = { flag = balnazzar_demanded_purity_tribute_cooldown years = 3 }

		every_vassal = {
			limit = { NOT = { faith = faith:scarletism } }
			add_opinion = { modifier = wc_purity_tribute_demanded target = root }
			random = {
				chance = 30
				set_character_faith_with_conversion = faith:scarletism
			}
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 20
	}
}

# Trigger 3 of 3 for wc_balnazzar_story.2000 - lets player strike first rather than wait to be exposed.
# Gated on high stress since nathrezim_disguise's stress_loss_mult already makes it hard to shed.
balnazzar_shed_the_mask_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/the_scarlet_crusade.dds" }
	decision_group_type = decisions

	ai_check_interval_by_tier = {
		barony = 0
		county = 0
		duchy = 12
		kingdom = 12
		empire = 12
		hegemony = 12
	}

	desc = balnazzar_shed_the_mask_decision_desc

	is_shown = {
		has_trait = nathrezim_disguise
		stress_level >= 2
		NOT = { has_character_flag = balnazzar_shed_the_mask }
	}

	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_incapable_trigger = no
	}

	# ck3 quirk: bare trigger_event with no other effect = blank Effects panel tooltip.
	# Wrap in custom_tooltip, same fix as purify_reclaimed above.
	effect = {
		custom_tooltip = balnazzar_shed_the_mask_decision_effect_tooltip

		trigger_event = {
			id = wc_balnazzar_story.2000
			days = 3
		}
	}

	ai_potential = {
		always = yes
	}

	ai_will_do = {
		base = 10

		modifier = {
			add = 40
			has_trait = wrathful
		}
		modifier = {
			add = 40
			has_trait = paranoid
		}
	}
}
