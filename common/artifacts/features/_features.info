Structure:

key = {
	group = feature group
	trigger = {} # Scopes are passed in from create_artifact and similar
	weight = script value # Same scopes as trigger
}

The artifact_feature_KEY is also used as a loc key in some contexts. E.G., "feature_sword_hilt_silver"
