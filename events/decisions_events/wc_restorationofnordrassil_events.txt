namespace = restorationofnordassil

restorationofnordassil.0001 = {
    type = character_event
	content_source = dlc_GOA
	title = restorationofnordassil_0001_title
	desc = restorationofnordassil_0001_desc
	theme = realm
	override_background = {
		reference = wc_background_restored_world_tree
	}

    immediate = {
        if = {
            limit = { title:b_nordrassil.title_province = { has_building = nordrassil_destroyed } }
            title:c_nordrassil.title_province = {
                remove_building = nordrassil_destroyed
                add_special_building = nordrassil_restored
            }
        }
        else = {
            limit = { title:b_nordrassil.title_province = { has_building = nordrassil_sacrificed } }
            title:c_nordrassil.title_province = {
                remove_building = nordrassil_sacrificed
                add_special_building = nordrassil_restored
            }
        }
    }

    option = {
        name = restorationofnordassil_0001_a

        hidden_effect = {
            every_living_character = {
                limit = { has_trait = creature_night_elf }

                add_character_modifier = {
                    modifier = night_elf_blessed_by_restored_nordrassil
                }
            }

            every_living_character = {
                limit = { has_trait = creature_dryad }

                add_character_modifier = {
                    modifier = dryad_and_keeper_blessed_by_restored_nordrassil
                }
            }
        }
    }
}
