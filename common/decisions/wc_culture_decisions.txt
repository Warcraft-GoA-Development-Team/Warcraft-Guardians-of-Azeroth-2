build_cosmic_observatory_decision = { 
	picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" } #TODO
	decision_group_type = major

	ai_check_interval = 36

	desc = build_cosmic_observatory_decision_desc

	is_shown = {
		culture = { has_cultural_pillar = heritage_highborne }
		is_landed = yes
		highest_held_title_tier >= tier_duchy
		OR = { # Show it earlier so it's something to work towards
			has_trait = lifestyle_mystic
			has_trait = scholar
			has_education_learning_trigger = yes
		}
		NOT = {
			top_liege = { 
				any_sub_realm_county = {
					has_variable = has_cosmic_observatory
				}
			}
		}
		
		# MAGIC 2024 TODO: void magic pathway
	}

	is_valid = {
		has_trait = lifestyle_mystic
		OR = { 
			has_trait = education_learning_5
			AND = { 
				OR = { 
					has_trait = education_learning_3
					has_trait = education_learning_4
				}
				has_trait = scholar
			}
			# MAGIC 2024 TODO: void magic pathway
		}
	}

	is_valid_showing_failures_only = { 
		custom_tooltip = {
			text = cant_be_hof
			NOT = {
				any_held_title = {
					is_head_of_faith = yes
				}
			}
		}
	}

	widget = {
		controller = create_holy_order
		gui = "decision_view_widget_place_special_building"
		barony_valid = {
			OR = {
				holder = scope:ruler
				AND = { 
					holder.primary_title.tier = tier_barony
					exists = holder.liege
					holder.liege = scope:ruler
				}
			}
			title_province = {
				has_special_building = no
				has_special_building_slot = no
			}
		}
	}

	effect = {
		scope:barony ?= {
			title_province = {
				add_special_building_slot = cosmic_observatory
			}
			county = {
				set_variable = has_cosmic_observatory
			}
		}
		
		add_character_flag = can_build_observatory
	}
}

become_demon_hunter_decision = { 
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_knight_kneeling.dds"
	}
	decision_group_type = major

	is_shown = {
		is_landless_advanturer = yes
		is_culture_or_parent_culture_trigger = {CULTURE = culture:night_elf}	
		OR = { 
			AND = { # can be any faith if they remember the sundering
				age > age_show_value
				faith = { like_pacifism_faith_trigger = no }
			}
			faith = faith:kaldorei_religion # otherwise should be kaldorei if babby
		}
		OR = { 
			is_illidan_trigger = yes
			exists = global_var:demon_hunters_returned # SS NOTE to ikari: use the debug menu to run 'effect = { set_demon_hunters_active = yes }' to test
			current_date > 615.1.30 # canon legion date TODO: replace after WOW:Legion invasion implemented
		}
		NOT = { has_trait = demon_hunter }
	}

	is_valid = { 
		OR = { # personality			# crime/negative
			has_trait = vengeful	has_trait = murderer
			has_trait = callous		has_trait = lunatic
			has_trait = cynical		has_trait = melancholy
			has_trait = wrathful	has_trait_xp = { trait = gallowsbait track = marauder value > 0 }
		}
		NOT = { 
			has_trait = zealous		has_trait = compassionate
			has_trait = calm		has_trait = forgiving
			has_trait = loyal		# you must be willing to sacrifice EVERYTHING
		}
		OR = { prowess > 20 intrigue > medium_skill_rating martial > medium_skill_rating }

	}

	ai_check_interval = 36

	effect = { 
		set_flag = in_training_demon_hunter
	}

}
